import { UnifiedEventView } from '@/components/events/UnifiedEventView';

/**
 * Discover Events page at /discover route
 * Uses UnifiedEventView component with grid/calendar toggle
 */
const DiscoverEvents = () => {
  return <UnifiedEventView />;
};

export default DiscoverEvents;
