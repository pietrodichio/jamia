---
phase: 12-e2e-testing
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - apps/web/e2e/tests/auth.spec.ts
autonomous: true

must_haves:
  truths:
    - "Sign-in with valid credentials redirects to /dashboard"
    - "Sign-in with wrong password shows error and stays on /auth"
    - "Sign-up with new email redirects to email confirmation page"
    - "Sign-up with existing email shows error"
    - "Unauthenticated user visiting /dashboard is redirected to /auth"
  artifacts:
    - path: "apps/web/e2e/tests/auth.spec.ts"
      provides: "Authentication E2E test suite"
      min_lines: 60
  key_links:
    - from: "apps/web/e2e/tests/auth.spec.ts"
      to: "apps/web/e2e/fixtures/index.ts"
      via: "import test and expect from fixtures"
      pattern: "from.*fixtures"
    - from: "apps/web/e2e/tests/auth.spec.ts"
      to: "apps/web/e2e/pages/AuthPage.ts"
      via: "authPage fixture provides POM"
      pattern: "authPage"
---

<objective>
Write E2E tests for authentication flows: sign-in, sign-up, error handling, and route protection.

Purpose: Auth is the most critical flow -- all other features depend on it. These tests verify the real auth flow against local Supabase.
Output: Passing auth.spec.ts with 5+ test cases covering sign-in, sign-up, errors, and route guards.
</objective>

<execution_context>
@/Users/pietrodichio/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pietrodichio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-e2e-testing/12-RESEARCH.md
@.planning/phases/12-e2e-testing/12-01-SUMMARY.md
@apps/web/src/pages/Auth.tsx
@apps/web/src/locales/it/common.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write authentication E2E test suite</name>
  <files>apps/web/e2e/tests/auth.spec.ts</files>
  <action>
Create `apps/web/e2e/tests/auth.spec.ts` using the custom fixtures from Plan 01.

IMPORTANT: Before writing tests, READ `apps/web/src/pages/Auth.tsx` and the relevant locale files to identify EXACT Italian labels, button text, and error messages used in the UI. The locators MUST match what the app actually renders.

The test file must:
1. Import `test, expect` from `../fixtures` (custom fixtures with Google Maps mock)
2. Use `test.use({ storageState: { cookies: [], origins: [] } })` at the top -- auth tests start unauthenticated
3. All locators use getByRole, getByLabel, or getByText with Italian text (never CSS selectors)

Test cases:

**describe('Sign In'):**
- `signs in with valid credentials and redirects to dashboard`: Use authPage fixture. Navigate to /auth, fill alice@example.com + password123, click login. Assert `page.toHaveURL` matches /dashboard (use timeout: 15_000 for redirect). Assert some dashboard content is visible.
- `shows error for invalid credentials`: Navigate to /auth, fill alice@example.com + wrongpassword, click login. Assert error text is visible (check what Supabase returns -- likely "Invalid login credentials" or Italian equivalent). Assert page stays on /auth.
- `shows error for non-existent email`: Fill nonexistent@example.com + password123. Assert error appears.

**describe('Sign Up'):**
- `registers new user and shows confirmation`: Navigate to /auth, toggle to signup mode, fill name + unique email (use `test-${Date.now()}@example.com`) + password + confirm password. Click register. Assert redirect to /email-confirmation or success message.
- `shows error for existing email`: Toggle to signup, fill alice@example.com (already exists). Assert error message.

**describe('Route Protection'):**
- `redirects unauthenticated user from /dashboard to /auth`: Navigate to /dashboard directly. Assert `page.toHaveURL` ends up at /auth.

Tips:
- Use generous timeouts (10-15s) for redirects -- Supabase auth + React render takes time
- The `authPage` fixture from custom fixtures provides the AuthPage POM
- For sign-up toggle, check Auth.tsx for the exact tab/button pattern (it might be tabs, not a toggle button)
  </action>
  <verify>
Run the full E2E suite with Supabase running locally:
```bash
cd apps/web && pnpm exec playwright test auth.spec.ts --project=mobile-chromium
```
All auth tests should pass. If Supabase is not running, start it first with `npx supabase start`.
  </verify>
  <done>
    - auth.spec.ts has 5+ test cases covering sign-in (valid + invalid), sign-up (new + duplicate), and route protection
    - All tests pass against local Supabase with seeded test users
    - Tests use Italian locators matching actual UI text
    - Tests use storageState: empty to start unauthenticated
  </done>
</task>

</tasks>

<verification>
1. `cd apps/web && pnpm exec playwright test auth.spec.ts` passes all tests
2. Test report shows all auth scenarios covered
3. Tests use AuthPage POM from fixtures for clean interaction code
</verification>

<success_criteria>
- All 5+ auth tests pass against local Supabase
- Sign-in, sign-up, error, and route guard scenarios verified
- No hardcoded waits (uses Playwright auto-waiting and expect assertions)
</success_criteria>

<output>
After completion, create `.planning/phases/12-e2e-testing/12-02-SUMMARY.md`
</output>
