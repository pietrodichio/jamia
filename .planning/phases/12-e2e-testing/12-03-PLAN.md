---
phase: 12-e2e-testing
plan: 03
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - apps/web/e2e/tests/event-creation.spec.ts
  - apps/web/e2e/pages/CreateEventPage.ts
autonomous: true

must_haves:
  truths:
    - "User can create a jam event through the wizard and land on event detail page"
    - "User can create a class event (includes teachers step) through the wizard"
    - "Wizard validates required fields and shows errors"
    - "Created event appears with correct title on the detail page"
  artifacts:
    - path: "apps/web/e2e/tests/event-creation.spec.ts"
      provides: "Event creation E2E test suite"
      min_lines: 80
    - path: "apps/web/e2e/pages/CreateEventPage.ts"
      provides: "Create event page object model"
      contains: "class CreateEventPage"
  key_links:
    - from: "apps/web/e2e/tests/event-creation.spec.ts"
      to: "apps/web/e2e/fixtures/index.ts"
      via: "import test and expect from fixtures"
      pattern: "from.*fixtures"
    - from: "apps/web/e2e/tests/event-creation.spec.ts"
      to: "apps/web/e2e/pages/CreateEventPage.ts"
      via: "CreateEventPage POM usage"
      pattern: "CreateEventPage"
---

<objective>
Write E2E tests for event creation wizard covering jam and class event types, form validation, and successful submission.

Purpose: Event creation is the primary write path -- testing the multi-step wizard end-to-end catches regression in form state, validation, and API submission.
Output: Passing event-creation.spec.ts with tests for jam creation, class creation (with teachers step), and validation errors.
</objective>

<execution_context>
@/Users/pietrodichio/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pietrodichio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-e2e-testing/12-RESEARCH.md
@.planning/phases/12-e2e-testing/12-01-SUMMARY.md
@apps/web/src/pages/CreateEvent.tsx
@apps/web/src/components/forms/event/EventTypeStep.tsx
@apps/web/src/components/forms/event/EventScheduleStep.tsx
@apps/web/src/components/forms/event/EventImageStep.tsx
@apps/web/src/components/forms/event/EventPreviewStep.tsx
@apps/web/src/components/forms/event/EventTeachersStep.tsx
@apps/web/src/locales/it/events.json
@apps/web/src/locales/it/forms.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CreateEventPage POM</name>
  <files>apps/web/e2e/pages/CreateEventPage.ts</files>
  <action>
IMPORTANT: Before writing the POM, READ the following files to understand the wizard structure, step names, button labels, and form field labels:
- `apps/web/src/pages/CreateEvent.tsx`
- `apps/web/src/components/forms/event/EventTypeStep.tsx`
- `apps/web/src/components/forms/event/EventScheduleStep.tsx`
- `apps/web/src/components/forms/event/EventImageStep.tsx`
- `apps/web/src/components/forms/event/EventPreviewStep.tsx`
- `apps/web/src/components/forms/event/EventTeachersStep.tsx`
- `apps/web/src/locales/it/events.json` and `apps/web/src/locales/it/forms.json`

Create `apps/web/e2e/pages/CreateEventPage.ts`:
- Class with constructor taking `Page`
- Locators for navigation: nextButton, prevButton, submitButton (match Italian text from locales)
- Locators for type step: event type cards (jam, class, workshop, convention)
- Locators for schedule step: title input, date fields, time fields
- Methods:
  - `goto()`: navigate to `/create-event`
  - `selectEventType(type: string)`: click the event type card and advance
  - `fillBasicInfo(opts: { title: string })`: fill title and any required fields on the schedule/details step
  - `skipImageStep()`: click next/skip on image step
  - `submitEvent()`: click the publish/create button on preview step
  - `getStepIndicator()`: return current step indicator text for assertions

Use getByRole and getByLabel with actual Italian labels from locale files. Handle the multi-step wizard flow where each step requires clicking "Avanti" (next) to advance.

NOTE: The wizard has conditional steps (teachers step for class/workshop/convention). The POM must handle both 4-step (jam) and 5-step flows.
  </action>
  <verify>
    File compiles without errors: `cd apps/web && npx tsc --noEmit 2>&1 | grep -i "CreateEventPage" || echo "No errors for CreateEventPage"`
  </verify>
  <done>
    - CreateEventPage POM covers wizard navigation (next, previous, submit)
    - Methods handle event type selection, basic info, image skip, and submission
    - All locators use Italian text from actual locale files
  </done>
</task>

<task type="auto">
  <name>Task 2: Write event creation E2E test suite</name>
  <files>apps/web/e2e/tests/event-creation.spec.ts</files>
  <action>
Create `apps/web/e2e/tests/event-creation.spec.ts` using custom fixtures.

This test file uses the DEFAULT storageState (alice.json) from playwright.config.ts -- DO NOT clear it. Tests need to be authenticated.

Test cases:

**describe('Event Creation - Jam'):**
- `creates a jam event through full wizard`:
  1. Navigate to /create-event
  2. Select "Jam" type, advance
  3. Fill title "Test Jam E2E", fill location (since Google Maps is mocked, type into the location field manually or find a way to set a location -- check how LocationInput works when Google Maps is unavailable)
  4. Fill start date/time with a future date
  5. Advance through steps (skip image)
  6. On preview step, verify title is shown
  7. Click submit/publish
  8. Assert redirect to event detail page (`/events/` URL pattern)
  9. Assert event title "Test Jam E2E" visible on detail page

**describe('Event Creation - Class'):**
- `creates a class event with teachers step`:
  1. Navigate to /create-event
  2. Select "Class" type (this adds the teachers step)
  3. Fill title "Test Class E2E", location, dates
  4. Advance steps (may skip teachers, skip image)
  5. Submit and verify redirect to event detail

**describe('Event Creation - Validation'):**
- `shows validation error when title is empty`:
  1. Navigate to /create-event
  2. Select "Jam" type, advance
  3. Try to advance without filling title
  4. Assert validation error message is visible
  5. Assert wizard stays on current step (does NOT advance)

IMPORTANT: The wizard auto-saves as draft. Tests should verify the FINAL submission (publish), not the auto-save. The event detail page after creation confirms the event was published.

Tips for location handling with mocked Google Maps:
- The LocationInput may have a text input that accepts manual text when Google Maps doesn't load
- Alternatively, the location field might require coordinates -- check if there's a fallback input
- If location is strictly required and Google Maps is mocked, the test may need to find the manual coordinate entry or description-only field
- READ the actual LocationInput component to understand fallback behavior
  </action>
  <verify>
Run tests:
```bash
cd apps/web && pnpm exec playwright test event-creation.spec.ts --project=mobile-chromium
```
All event creation tests should pass.
  </verify>
  <done>
    - event-creation.spec.ts has 3+ test cases covering jam creation, class creation, and validation
    - Tests run authenticated as alice (storageState from config)
    - Created events redirect to detail page with correct title
    - Wizard multi-step flow is exercised end-to-end
  </done>
</task>

</tasks>

<verification>
1. `cd apps/web && pnpm exec playwright test event-creation.spec.ts` passes all tests
2. Tests create real events in local Supabase (not mocked)
3. Event detail page shows created event data
</verification>

<success_criteria>
- Jam and class event creation flows tested end-to-end
- Wizard validation catches missing required fields
- Created events are verifiable on the detail page
- Location handling works with mocked Google Maps
</success_criteria>

<output>
After completion, create `.planning/phases/12-e2e-testing/12-03-SUMMARY.md`
</output>
