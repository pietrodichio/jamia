---
phase: 12-e2e-testing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/playwright.config.ts
  - apps/web/e2e/setup/db.setup.ts
  - apps/web/e2e/setup/auth.setup.ts
  - apps/web/e2e/fixtures/index.ts
  - apps/web/e2e/pages/AuthPage.ts
  - apps/web/package.json
  - apps/web/.gitignore
autonomous: true

must_haves:
  truths:
    - "pnpm --filter @jamia/web exec playwright test --list shows available test projects"
    - "DB reset setup project resets Supabase database via supabase db reset"
    - "Auth setup project authenticates alice and bob via REST API and saves storageState for each"
    - "Google Maps requests are mocked globally to prevent test failures"
  artifacts:
    - path: "apps/web/playwright.config.ts"
      provides: "Playwright configuration with db-reset, auth-setup, and mobile-chromium projects"
      contains: "defineConfig"
    - path: "apps/web/e2e/setup/db.setup.ts"
      provides: "Database reset setup project"
      contains: "supabase db reset"
    - path: "apps/web/e2e/setup/auth.setup.ts"
      provides: "Auth setup via Supabase REST API with storageState save for alice and bob"
      contains: "sb-fuddhnsjxumqhdquvchx-auth-token"
    - path: "apps/web/e2e/fixtures/index.ts"
      provides: "Custom test fixtures with AuthPage POM and Google Maps mock"
      contains: "base.extend"
    - path: "apps/web/e2e/pages/AuthPage.ts"
      provides: "Auth page object model for login/signup interactions"
      contains: "class AuthPage"
  key_links:
    - from: "apps/web/playwright.config.ts"
      to: "apps/web/e2e/setup/db.setup.ts"
      via: "project dependency chain"
      pattern: "db-reset.*auth-setup.*mobile-chromium"
    - from: "apps/web/e2e/setup/auth.setup.ts"
      to: "apps/web/.auth/alice.json"
      via: "storageState save"
      pattern: "storageState.*alice\\.json"
    - from: "apps/web/e2e/setup/auth.setup.ts"
      to: "apps/web/.auth/bob.json"
      via: "storageState save"
      pattern: "storageState.*bob\\.json"
---

<objective>
Set up Playwright E2E testing infrastructure with config, setup projects, fixtures, and first page object model.

Purpose: Establish the foundation all E2E test specs depend on -- Playwright config with sequential execution, DB reset setup, auth via Supabase REST API, custom fixtures with Google Maps mock, and the AuthPage POM.
Output: Working Playwright setup that can reset DB, authenticate, and run tests on iPhone 14 viewport.
</objective>

<execution_context>
@/Users/pietrodichio/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pietrodichio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-e2e-testing/12-RESEARCH.md
@apps/web/package.json
@apps/web/.gitignore
@apps/web/supabase/config.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Playwright and create config + setup projects</name>
  <files>
    apps/web/package.json
    apps/web/playwright.config.ts
    apps/web/e2e/setup/db.setup.ts
    apps/web/e2e/setup/auth.setup.ts
    apps/web/.gitignore
  </files>
  <action>
1. Install Playwright as devDependency:
   ```bash
   cd apps/web && pnpm add -D @playwright/test && npx playwright install chromium
   ```

2. Add scripts to `apps/web/package.json`:
   ```json
   "test:e2e": "playwright test",
   "test:e2e:ui": "playwright test --ui",
   "test:e2e:headed": "playwright test --headed"
   ```

3. Create `apps/web/playwright.config.ts`:
   - `testDir: './e2e/tests'`
   - `fullyParallel: false`, `workers: 1` (sequential execution per CONTEXT decisions)
   - `forbidOnly: !!process.env.CI`
   - `retries: process.env.CI ? 1 : 0`
   - Reporter: CI uses `[['github'], ['html', { outputFolder: 'playwright-report', open: 'never' }]]`, local uses `[['html', { open: 'on-failure' }], ['list']]`
   - `use.baseURL: 'http://localhost:8080'` (Vite dev server)
   - `use.screenshot: 'only-on-failure'`
   - `use.video: 'retain-on-failure'`
   - `use.trace: 'on-first-retry'`
   - `use.locale: 'it-IT'`
   - Three projects in dependency chain:
     a. `db-reset`: `testMatch: /db\.setup\.ts/`, testDir: `'./e2e/setup'`
     b. `auth-setup`: `testMatch: /auth\.setup\.ts/`, testDir: `'./e2e/setup'`, `dependencies: ['db-reset']`
     c. `mobile-chromium`: uses `devices['iPhone 14']`, `storageState: path.join(__dirname, '.auth/alice.json')`, `dependencies: ['auth-setup']`
   - `webServer`: `command: 'pnpm run dev'` (starts Vite), `url: 'http://localhost:8080'`, `reuseExistingServer: !process.env.CI`, `timeout: 120_000`
   - IMPORTANT: Setup projects must have their own `testDir: './e2e/setup'` so they can be found. The main test project uses `testDir: './e2e/tests'` from the top-level config.

4. Create `apps/web/e2e/setup/db.setup.ts`:
   - Import `test as setup` from `@playwright/test`
   - Import `execSync` from `child_process` and `path`
   - Resolve `webDir` as `path.resolve(__dirname, '../../')` (apps/web)
   - Run `npx supabase db reset --yes` with `cwd: webDir`, `stdio: 'pipe'`, `timeout: 120_000`
   - NOTE: `supabase db reset` automatically applies all migrations AND runs `supabase/seed.sql`. Do NOT call any separate seed scripts (seed-users.ts and seed-events.ts do not exist in this repo). The seed.sql file handles all test data seeding.
   - Wrap in try/catch, log progress with console.log

5. Create `apps/web/e2e/setup/auth.setup.ts`:
   - Import `test as setup, expect` from `@playwright/test`
   - Constants: `SUPABASE_URL = 'http://127.0.0.1:54421'`, `SUPABASE_ANON_KEY` = the local dev anon key (from RESEARCH.md -- safe to hardcode, it's the local dev key)
   - AUTH_DIR = `path.resolve(__dirname, '../../.auth')`
   - Export `ALICE_AUTH_STATE = path.join(AUTH_DIR, 'alice.json')`
   - Export `BOB_AUTH_STATE = path.join(AUTH_DIR, 'bob.json')`
   - Create a helper function `authenticateUser(page, email, password, statePath)` that:
     a. Creates `.auth/` directory if not exists
     b. POSTs to `${SUPABASE_URL}/auth/v1/token?grant_type=password` with email/password
     c. Asserts response.ok
     d. Navigates page to `/` (needed to set localStorage on correct origin)
     e. Uses `page.evaluate()` to set localStorage key `sb-fuddhnsjxumqhdquvchx-auth-token` with session data (access_token, refresh_token, expires_at, token_type, user)
     f. Saves `page.context().storageState({ path: statePath })`
   - Setup test "authenticate as alice": calls `authenticateUser(page, 'alice@example.com', 'password123', ALICE_AUTH_STATE)`
   - Setup test "authenticate as bob": calls `authenticateUser(page, 'bob@example.com', 'password123', BOB_AUTH_STATE)`

6. Update `apps/web/.gitignore` -- append:
   ```
   # Playwright
   .auth/
   playwright-report/
   test-results/
   ```
  </action>
  <verify>
    Run `cd apps/web && pnpm exec playwright test --list` to confirm Playwright recognizes the config and projects. Should list db-reset, auth-setup, and mobile-chromium projects without errors.
  </verify>
  <done>
    - Playwright installed as devDependency in apps/web
    - playwright.config.ts exists with 3 projects (db-reset -> auth-setup -> mobile-chromium)
    - db.setup.ts resets database via `supabase db reset --yes` (which auto-applies seed.sql)
    - auth.setup.ts authenticates alice AND bob via REST API and saves storageState for each
    - .gitignore includes .auth/, playwright-report/, test-results/
    - package.json has test:e2e, test:e2e:ui, test:e2e:headed scripts
  </done>
</task>

<task type="auto">
  <name>Task 2: Create custom fixtures, AuthPage POM, and Google Maps mock</name>
  <files>
    apps/web/e2e/fixtures/index.ts
    apps/web/e2e/pages/AuthPage.ts
  </files>
  <action>
1. Create `apps/web/e2e/pages/AuthPage.ts`:
   - Page Object Model class with constructor taking `Page`
   - Locators (use getByLabel and getByRole with Italian text):
     - `emailInput`: `page.getByLabel('Email')` (works for both login and signup since label is same)
     - `passwordInput`: `page.getByLabel('Password', { exact: true })` (exact to distinguish from confirm)
     - `loginButton`: `page.getByRole('button', { name: /Accedi/i })`
     - `signupLink`: `page.getByText(/Non hai un account/i)` or appropriate toggle text
     - `nameInput`: `page.getByLabel(/Nome/i)` (signup mode)
     - `confirmPasswordInput`: `page.getByLabel(/Conferma/i)`
     - `registerButton`: `page.getByRole('button', { name: /Registrati/i })`
   - Methods:
     - `goto()`: navigate to `/auth`
     - `signIn(email, password)`: fill email, fill password, click login
     - `signUp(name, email, password)`: click signup toggle, fill name/email/password/confirm, click register
   - NOTE: Check the actual Auth.tsx component to verify label text. The app is in Italian. Read `apps/web/src/pages/Auth.tsx` and locales to confirm exact labels.

2. Create `apps/web/e2e/fixtures/index.ts`:
   - Import `test as base` from `@playwright/test`
   - Import `AuthPage` from `../pages/AuthPage`
   - Import `ALICE_AUTH_STATE` from `../setup/auth.setup`
   - Define Fixtures type: `{ authPage: AuthPage }`
   - Export `test = base.extend<Fixtures>({ authPage: async ({ page }, use) => { await use(new AuthPage(page)); } })`
   - Export `{ expect }` from `@playwright/test`
   - Add a global `beforeEach` in fixtures that mocks Google Maps:
     Within the test extension, add an `autoMockGoogleMaps` fixture (auto: true) that intercepts `**/maps.googleapis.com/**` routes and fulfills with empty JSON response. This prevents Google Maps from breaking any test that loads pages using it.
     ```typescript
     autoMockGoogleMaps: [async ({ page }, use) => {
       await page.route('**/maps.googleapis.com/**', route => {
         route.fulfill({ status: 200, contentType: 'application/json', body: JSON.stringify({ predictions: [], status: 'ZERO_RESULTS' }) });
       });
       await use();
     }, { auto: true }],
     ```
  </action>
  <verify>
    Verify the files compile by running `cd apps/web && npx tsc --noEmit --project tsconfig.json 2>&1 | head -20` (may have some unrelated errors, but the e2e files should not produce errors). Alternatively check that `pnpm exec playwright test --list` still works without parse errors.
  </verify>
  <done>
    - AuthPage POM exists with goto(), signIn(), signUp() methods using Italian labels
    - Custom fixtures export `test` with authPage fixture and auto Google Maps mock
    - All E2E infrastructure ready for test specs in Wave 2
  </done>
</task>

</tasks>

<verification>
1. `cd apps/web && pnpm exec playwright test --list` shows projects without errors
2. File structure exists: `e2e/setup/db.setup.ts`, `e2e/setup/auth.setup.ts`, `e2e/fixtures/index.ts`, `e2e/pages/AuthPage.ts`
3. `playwright.config.ts` has 3 projects with correct dependency chain
4. `.gitignore` includes Playwright artifacts
</verification>

<success_criteria>
- Playwright infrastructure is fully set up and recognizable by the test runner
- Setup projects will reset DB and authenticate when tests run
- Google Maps is automatically mocked to prevent third-party failures
- AuthPage POM is ready for use by auth test specs
- Both alice.json and bob.json storageState files are produced by auth setup
</success_criteria>

<output>
After completion, create `.planning/phases/12-e2e-testing/12-01-SUMMARY.md`
</output>
