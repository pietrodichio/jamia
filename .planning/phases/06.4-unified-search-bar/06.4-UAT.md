# Phase 6.4 UAT: Unified Search Bar

**Created:** 2026-01-29
**Status:** In Progress

## Test Summary

| # | Test | Status |
|---|------|--------|
| 1 | Unified search bar structure | passed |
| 2 | IP location auto-prefill | passed |
| 3 | GPS location request | passed |
| 4 | City autocomplete | re-test |
| 5 | Keyword real-time search | pending |
| 6 | Location search on button only | pending |
| 7 | Mobile drawer pattern | pending |
| 8 | City name display format | pending |

**Passed:** 3 | **Failed:** 0 | **Pending:** 5

---

## Current Test

### Test 4: City autocomplete (RE-TEST #2 after Google Maps fix)

**Issues fixed:**
1. Dropdown positioned relative to location section (user adjusted width)
2. Fixed Google Maps Places loading:
   - `useGoogleMaps.ts`: Proper `importLibrary('places')` call
   - `google-maps.ts`: Use `loading=async` parameter for modern API

**What to test:**
Verify that typing a city name shows Google Places autocomplete suggestions.

**Expected behavior:**
- Click location input to open dropdown
- Type a city name (e.g., "Mila") in the search field
- Autocomplete suggestions appear below (cities only)
- Select a city - it appears in location input BUT events don't update yet

**Steps:**
1. Hard refresh (`Cmd+Shift+R`) - important to clear cached Google Maps script
2. Open browser DevTools console
3. Navigate to `/discover`
4. Click on the location input field
5. Type "Mila" in the search field
6. Wait for autocomplete suggestions (should see network calls to Google)
7. If suggestions appear, select one
8. Verify city name appears in location input
9. Verify events have NOT changed (location is staged)

**Reply:** `pass` if autocomplete works, `fail` with console output if issues persist

---

## Pending Tests

### Test 5: Keyword real-time search
- Type in keyword input
- Events filter in real-time with debounce (no search button needed)
- Pressing Enter also works for search

### Test 6: Location search on button only
- Select a new city from autocomplete
- Verify the selection shows in input BUT events don't update yet
- Click the circular search button
- Verify NOW the events update based on new location

### Test 7: Mobile drawer pattern
- Resize to mobile viewport (< 768px) or use mobile device
- Verify compact trigger button appears instead of inline bar
- Tap trigger to open drawer/modal
- Verify vertical stacked inputs inside drawer
- Search button in drawer closes it and executes search

### Test 8: City name display format
- Verify all location displays show city name only (e.g., "Milano")
- NOT full format like "Milano, Italia" or "Milano, Lombardia, Italia"
- Consistent across IP, GPS, and city search results

---

## Test Results Log

| Test | Time | Result | Notes |
|------|------|--------|-------|
| 1. Unified search bar structure | 2026-01-29 | passed | Layout matches Eventbrite-style |
| 2. IP location auto-prefill | 2026-01-29 | passed | User removed "(approssimativa)" indicator and ring styling |
| 3. GPS location request | 2026-01-29 | passed | Fixed: improved error handling with fallback, added relative positioning |
| 4. City autocomplete | 2026-01-29 | failed | No network call, dropdown too wide. **Fixed:** moved dropdown inside location section, fixed width, fixed Google Maps loading (useGoogleMaps + loadGoogleMaps with loading=async) |
