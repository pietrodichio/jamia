---
phase: 08-event-recap-email-engine
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - apps/backend/src/digest/templates/DigestEmail.tsx
  - apps/backend/src/digest/templates/components/EventCard.tsx
  - apps/backend/src/digest/templates/components/EmailHeader.tsx
  - apps/backend/src/digest/templates/components/EmailFooter.tsx
  - apps/backend/src/digest/digest.service.ts
autonomous: true

must_haves:
  truths:
    - "DigestEmail template renders valid HTML with two sections: Prossimi eventi and Nuovi eventi"
    - "EventCard displays image thumbnail, title, date, city, and link"
    - "EmailHeader shows Jamia logo only (minimal header)"
    - "EmailFooter contains unsubscribe link, explanation text, and manage preferences link"
    - "Template renders to both HTML and plain text"
    - "Type groups render in priority order: conventions first, workshops second, jams third"
    - "Sections with no events are omitted from the email"
    - "Italian text used throughout (Prossimi eventi, Nuovi eventi, footer text)"
  artifacts:
    - path: "apps/backend/src/digest/templates/DigestEmail.tsx"
      provides: "Main email template with sections for upcoming and new events"
      min_lines: 40
    - path: "apps/backend/src/digest/templates/components/EventCard.tsx"
      provides: "Event display card with image, title, date, city, link"
      min_lines: 20
    - path: "apps/backend/src/digest/templates/components/EmailHeader.tsx"
      provides: "Minimal header with Jamia logo"
      min_lines: 10
    - path: "apps/backend/src/digest/templates/components/EmailFooter.tsx"
      provides: "Footer with unsubscribe, explanation, manage preferences"
      min_lines: 15
  key_links:
    - from: "apps/backend/src/digest/templates/DigestEmail.tsx"
      to: "apps/backend/src/digest/dto/curated-events.dto.ts"
      via: "Import CuratedEvents type for props"
      pattern: "CuratedEvents"
    - from: "apps/backend/src/digest/digest.service.ts"
      to: "apps/backend/src/digest/templates/DigestEmail.tsx"
      via: "render() function call"
      pattern: "render.*DigestEmail"
---

<objective>
Create React Email templates for the digest email and add rendering methods to the digest service that produce both HTML and plain text output.

Purpose: The templates define what users see in their inbox. They must match Jamia branding, display events in the correct structure (two sections, type priority), and include compliant footer with unsubscribe/preferences links. The rendering methods are the bridge between curation data and sendable email content.
Output: Complete email template component hierarchy and renderDigest() method on DigestService.
</objective>

<execution_context>
@/Users/pietrodichio/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pietrodichio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-event-recap-email-engine/08-CONTEXT.md
@.planning/phases/08-event-recap-email-engine/08-RESEARCH.md
@.planning/phases/08-event-recap-email-engine/08-01-SUMMARY.md

Key existing files:
@apps/backend/src/digest/digest.service.ts
@apps/backend/src/digest/dto/curated-events.dto.ts
@apps/backend/src/email/email.service.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create React Email template components</name>
  <files>
    apps/backend/src/digest/templates/components/EventCard.tsx
    apps/backend/src/digest/templates/components/EmailHeader.tsx
    apps/backend/src/digest/templates/components/EmailFooter.tsx
    apps/backend/src/digest/templates/DigestEmail.tsx
  </files>
  <action>
    Create the email template component hierarchy using @react-email/components and @react-email/tailwind.

    1. Create `EmailHeader.tsx`:
       - Use Section, Img from @react-email/components
       - Centered Jamia logo (use placeholder URL: https://jamia.app/logo.png -- can be updated later)
       - Width 120px, auto margins
       - Minimal: just the logo, no text or banner
       - Keep styling simple for email client compatibility

    2. Create `EmailFooter.tsx`:
       - Props: unsubscribeToken (string), frontendBaseUrl (string)
       - Use Section, Text, Link from @react-email/components
       - Content (all in Italian):
         - Horizontal rule or border-top separator
         - "Ricevi questa email perche sei iscritto a Jamia."
         - Unsubscribe link: "Annulla iscrizione" -> `${frontendBaseUrl}/unsubscribe?token=${unsubscribeToken}`
         - Separator dot
         - Manage preferences link: "Gestisci preferenze" -> `${frontendBaseUrl}/profile` (where EmailPreferencesCard lives)
       - Small text size (12-13px), muted gray color (#6b7280)

    3. Create `EventCard.tsx`:
       - Props: event (DigestEvent type from dto), frontendBaseUrl (string)
       - Import DigestEvent from the dto file
       - Layout: horizontal card with image on left (if available), text on right
       - If image_url exists: show thumbnail (80x80 or similar, rounded corners -- note Outlook may render as square, that's acceptable)
       - Title: bold, linked to `${frontendBaseUrl}/events/${event.id}`
       - Date: format starts_at using Intl.DateTimeFormat('it-IT', { dateStyle: 'long', timeStyle: 'short', timeZone: 'Europe/Rome' })
         - Important: since this runs at build/render time on the server, format the date in the template props, NOT in JSX. Pass pre-formatted strings as props OR format inside the component. The render() call happens server-side so Intl works fine.
       - City: show location_city if available
       - Use Row/Column from @react-email/components for horizontal layout, OR use a simpler stacked layout (image above, text below) for better email client compatibility
       - Prefer the simpler stacked approach: image (full width, max-height 160px, rounded), then title, date, city below
       - Add bottom margin between cards (16-20px)
       - Link wraps the entire card or just the title

    4. Create `DigestEmail.tsx`:
       - Props interface: { curated: CuratedEvents, userName: string | null, unsubscribeToken: string, frequency: 'weekly' | 'monthly', frontendBaseUrl: string }
       - Use Html, Head, Body, Container, Section, Text, Preview from @react-email/components
       - Wrap in Tailwind from @react-email/tailwind
       - Structure:
         a. Preview text (hidden): "I tuoi eventi questa settimana" (weekly) or "I tuoi eventi del mese" (monthly)
         b. Body with light background (bg-gray-50 or #f9fafb)
         c. Container max-width 600px, centered, white background
         d. EmailHeader component
         e. Greeting: "Ciao {userName}," or "Ciao," if no name
         f. Intro text based on frequency:
            - Weekly: "Ecco gli eventi della settimana per te."
            - Monthly: "Ecco gli eventi del mese per te."
         g. "Prossimi eventi" section:
            - Only render if at least one group has events
            - Section heading: "Prossimi eventi" (Text, bold, larger font)
            - Render type groups in order: conventions, workshops, jams
            - For each non-empty group, show type subheading (e.g., "Convention", "Workshop", "Jam") and EventCards
            - Use Italian type labels: "Convention", "Workshop", "Jam" (these are already used as-is in Italian AcroYoga community)
         h. "Nuovi eventi" section:
            - Same structure as above
            - Section heading: "Nuovi eventi"
            - Only render if at least one group has events
         i. EmailFooter component

       Design notes matching Jamia branding:
       - Use primary brand color for headings/links (check homepage -- likely a warm/accent color. Use #6366f1 indigo as safe default, can be adjusted)
       - Clean, generous whitespace
       - Font: system font stack (-apple-system, BlinkMacSystemFont, 'Segoe UI', etc.)
       - Keep total email compact (aim for <50KB HTML to stay well under Gmail's 102KB clip threshold)
  </action>
  <verify>
    - All 4 template files exist and contain valid TSX
    - `pnpm run build` in apps/backend succeeds (TSX compilation works)
    - DigestEmail imports and uses EventCard, EmailHeader, EmailFooter
    - EventCard imports DigestEvent type
    - Footer includes unsubscribe link and manage preferences link
    - Both sections ("Prossimi eventi", "Nuovi eventi") conditionally render based on content
  </verify>
  <done>Complete React Email template hierarchy renders digest emails with two sections, event cards, header, and compliant footer -- all in Italian</done>
</task>

<task type="auto">
  <name>Task 2: Add rendering methods to digest service</name>
  <files>
    apps/backend/src/digest/digest.service.ts
  </files>
  <action>
    Add rendering methods to the existing DigestService that convert React Email templates to HTML and plain text.

    1. Add import for `render` from `@react-email/render` and import `DigestEmail` from templates
    2. Add `frontendBaseUrl` property initialized from ConfigService (inject ConfigService in constructor):
       - Read from `FRONTEND_BASE_URL` env var
       - Fallback to 'https://jamia.app'
    3. Add method `renderDigest(curated: CuratedEvents, user: SubscribedUser, frequency: 'weekly' | 'monthly'): Promise<{ html: string; text: string; subject: string }>`:
       - Determine subject line:
         - Weekly: "I tuoi eventi della settimana su Jamia"
         - Monthly: "I tuoi eventi del mese su Jamia"
       - Create DigestEmail React element with props:
         - curated
         - userName: user.first_name
         - unsubscribeToken: user.unsubscribe_token
         - frequency
         - frontendBaseUrl
       - Call `render(element)` for HTML
       - Call `render(element, { plainText: true })` for plain text
       - Log the HTML size in bytes for monitoring (warn if >80KB):
         `this.logger.log('[DigestService] Rendered digest HTML: ${Buffer.byteLength(html)} bytes')`
         If >80KB: `this.logger.warn('[DigestService] Digest HTML exceeds 80KB, risk of Gmail clipping')`
       - Return { html, text, subject }

    4. Import ConfigService from @nestjs/config and add to constructor injection.
  </action>
  <verify>
    - `pnpm run build` succeeds
    - digest.service.ts has renderDigest method
    - Method produces both HTML and plain text strings
    - Subject line varies by frequency
    - HTML size logging is present
  </verify>
  <done>DigestService can render curated events into HTML and plain text email content with appropriate subject lines</done>
</task>

</tasks>

<verification>
1. `cd apps/backend && pnpm run build` -- full backend compile succeeds with TSX templates
2. Template files exist: DigestEmail.tsx, EventCard.tsx, EmailHeader.tsx, EmailFooter.tsx
3. DigestService.renderDigest() method exists and imports render from @react-email/render
4. Templates use Italian text throughout
5. Footer includes unsubscribe and manage preferences links
</verification>

<success_criteria>
- Backend compiles successfully with all TSX template files
- DigestEmail template conditionally renders Prossimi eventi and Nuovi eventi sections
- EventCard shows event title, date (Italian format), city, and image
- Footer has unsubscribe link and manage preferences link with correct URLs
- renderDigest() produces both HTML and plain text output
- Subject lines are in Italian and vary by frequency
</success_criteria>

<output>
After completion, create `.planning/phases/08-event-recap-email-engine/08-02-SUMMARY.md`
</output>
