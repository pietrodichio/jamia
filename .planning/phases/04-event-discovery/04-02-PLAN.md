---
phase: 04-event-discovery
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - apps/backend/src/events/dto/search-events.dto.ts
  - apps/backend/src/events/events.service.ts
  - apps/backend/src/events/events.controller.ts
autonomous: true

must_haves:
  truths:
    - "Backend accepts location-based search requests"
    - "Backend returns events sorted by distance"
    - "Backend applies filters (type, date range, keyword)"
    - "Backend validates search parameters"
  artifacts:
    - path: "apps/backend/src/events/dto/search-events.dto.ts"
      provides: "SearchEventsDto with validation"
      min_lines: 40
      exports: ["SearchEventsDto"]
    - path: "apps/backend/src/events/events.service.ts"
      provides: "searchEvents method calling RPC function"
      contains: "search_events_by_location"
    - path: "apps/backend/src/events/events.controller.ts"
      provides: "GET /events/search endpoint"
      contains: "@Get('search')"
  key_links:
    - from: "EventsController.searchEvents"
      to: "EventsService.searchEvents"
      via: "method call with DTO"
      pattern: "this\\.eventsService\\.searchEvents"
    - from: "EventsService.searchEvents"
      to: "Supabase RPC function"
      via: "supabase.rpc('search_events_by_location')"
      pattern: "\\.rpc\\('search_events_by_location'"
---

<objective>
Create backend API endpoint for location-based event search with filtering.

Purpose: Expose the PostGIS geospatial search capabilities via REST API, allowing frontend to search events by location, distance, type, date range, and keyword. Validates input parameters and returns type-safe results.

Output: SearchEventsDto for validation, service method calling Supabase RPC function, and controller endpoint at GET /events/search.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-event-discovery/04-RESEARCH.md
@.planning/phases/03-event-foundation/03-02-SUMMARY.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@apps/backend/src/events/events.service.ts
@apps/backend/src/events/events.controller.ts
@apps/backend/src/events/dto/create-event.dto.ts
@packages/types/src/event.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SearchEventsDto with validation</name>
  <files>apps/backend/src/events/dto/search-events.dto.ts</files>
  <action>
Create DTO with class-validator decorators:

**Required location parameters:**
- lng: number (@IsNumber())
- lat: number (@IsNumber())

**Optional filter parameters:**
- radius: number (@IsNumber(), @Min(100), @Max(500000), @IsOptional(), @Type(() => Number)) - defaults to 50000 meters (50km)
- types: string[] (@IsArray(), @IsString({ each: true }), @IsOptional()) - filter by event types
- dateFrom: string (@IsDateString(), @IsOptional()) - filter starts_at >= dateFrom
- dateTo: string (@IsDateString(), @IsOptional()) - filter starts_at <= dateTo
- keyword: string (@IsString(), @IsOptional()) - full-text search in title/description

Add class-level validation:
- Import Transform, Type from class-transformer
- Use @Type(() => Number) for numeric query params (they arrive as strings)
- Use @IsIn(['jam', 'class', 'workshop', 'convention'], { each: true }) for types validation

Follow existing DTO patterns from create-event.dto.ts (same validation style, same imports).
  </action>
  <verify>
File contains:
- Class SearchEventsDto exported
- All required and optional fields defined
- class-validator decorators on all fields
- @Type(() => Number) on numeric fields
- @IsIn validation for event types array
- Imports from class-validator and class-transformer

Run: pnpm --filter @jamia/backend build
Ensure no TypeScript errors.
  </verify>
  <done>
SearchEventsDto created with comprehensive validation for all search parameters, ensuring type safety and constraint enforcement at API boundary.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add searchEvents method to EventsService</name>
  <files>apps/backend/src/events/events.service.ts</files>
  <action>
Add searchEvents method to EventsService class:

```typescript
async searchEvents(dto: SearchEventsDto) {
  const { data, error } = await this.supabase
    .rpc('search_events_by_location', {
      search_lng: dto.lng,
      search_lat: dto.lat,
      radius_meters: dto.radius || 50000,
      event_types: dto.types || null,
      date_from: dto.dateFrom || null,
      date_to: dto.dateTo || null,
      keyword: dto.keyword || null
    });

  if (error) {
    throw new Error(`Failed to search events: ${error.message}`);
  }

  return data;
}
```

Import SearchEventsDto at top of file.

Follow existing service patterns - same error handling style, same Supabase call structure as other methods in this file.

Return type will be inferred from RPC function result (array of events with distance_meters).
  </action>
  <verify>
File contains:
- searchEvents method with SearchEventsDto parameter
- Calls supabase.rpc('search_events_by_location', {...})
- Error handling with descriptive message
- Returns data from RPC call
- Import for SearchEventsDto added

Run: pnpm --filter @jamia/backend build
Ensure no TypeScript errors.
  </verify>
  <done>
EventsService.searchEvents method implemented, calling PostGIS RPC function via Supabase client with proper error handling.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add search endpoint to EventsController</name>
  <files>apps/backend/src/events/events.controller.ts</files>
  <action>
Add search endpoint to EventsController:

```typescript
@Get('search')
async searchEvents(@Query() dto: SearchEventsDto) {
  return this.eventsService.searchEvents(dto);
}
```

Place this endpoint BEFORE @Get(':id') to avoid route conflicts (search is more specific).

Import Query decorator from @nestjs/common and SearchEventsDto at top of file.

Use @Query() decorator (not @Body) because this is a GET request - query params auto-validate against DTO with ValidationPipe.

NO authentication guard needed - published events are public per requirements (LOC-01: "User can search events").
  </action>
  <verify>
File contains:
- @Get('search') endpoint
- @Query() decorator with SearchEventsDto
- Calls this.eventsService.searchEvents(dto)
- Positioned before @Get(':id') route
- Imports for Query and SearchEventsDto added

Run: pnpm --filter @jamia/backend build
Ensure no TypeScript errors.

Test endpoint accessibility:
curl http://localhost:8088/events/search?lng=-122.4194&lat=37.7749&radius=50000
(Should return 200, even if empty array - validates route works)
  </verify>
  <done>
GET /events/search endpoint created, accepting validated query parameters and returning location-sorted events with distance information.
  </done>
</task>

</tasks>

<verification>
- SearchEventsDto validates all parameters with appropriate constraints
- EventsService.searchEvents calls correct RPC function with mapped parameters
- EventsController exposes /events/search as GET endpoint
- Route ordering prevents conflicts with :id route
- No authentication required (public event search)
- TypeScript compilation succeeds with no errors
- Backend can process location-based searches with filters
</verification>

<success_criteria>
- GET /events/search?lng=X&lat=Y returns published events sorted by distance
- Query params are validated (invalid types/ranges rejected with 400)
- Optional filters work: types, dateFrom, dateTo, keyword
- Distance is included in response (distance_meters field)
- Empty location returns appropriate error (DTO validation fails)
- Backend successfully calls PostGIS RPC function
</success_criteria>

<output>
After completion, create `.planning/phases/04-event-discovery/04-02-SUMMARY.md` with:
- API endpoint signature and example request
- DTO validation rules
- RPC function integration approach
- Response format with distance field
- Any deviations from plan
</output>
