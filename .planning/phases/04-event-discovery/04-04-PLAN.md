---
phase: 04-event-discovery
plan: 04
type: execute
wave: 3
depends_on: [04-02, 04-03]
files_modified:
  - apps/web/src/pages/DiscoverEvents.tsx
  - apps/web/src/components/events/EventList.tsx
  - apps/web/src/components/events/EventCard.tsx
  - apps/web/src/components/events/EventFilters.tsx
  - apps/web/src/components/search/LocationSearch.tsx
  - apps/web/src/components/search/KeywordSearch.tsx
  - apps/web/src/App.tsx
autonomous: true

must_haves:
  truths:
    - "User can search events by entering city name or using geolocation"
    - "User can filter events by type (jam, class, workshop, convention)"
    - "User can filter events by date range"
    - "User can search events by keyword in title/description"
    - "Search results show events sorted by distance"
    - "User can see distance to each event"
  artifacts:
    - path: "apps/web/src/pages/DiscoverEvents.tsx"
      provides: "Main event discovery page with search and list"
      min_lines: 80
    - path: "apps/web/src/components/events/EventList.tsx"
      provides: "Event list component with React Query"
      min_lines: 50
    - path: "apps/web/src/components/events/EventCard.tsx"
      provides: "Individual event display with distance"
      min_lines: 60
    - path: "apps/web/src/components/events/EventFilters.tsx"
      provides: "Filter UI controls"
      min_lines: 100
  key_links:
    - from: "DiscoverEvents page"
      to: "EventList component"
      via: "props with filters"
      pattern: "<EventList.*filters="
    - from: "EventList"
      to: "eventsApi.searchEvents"
      via: "React Query useQuery"
      pattern: "useQuery.*searchEvents"
    - from: "KeywordSearch"
      to: "useEventFilters"
      via: "debounced input"
      pattern: "useDebouncedCallback"
---

<objective>
Create event discovery list view with location search, filtering, and keyword search.

Purpose: Enable users to find events by location and apply multiple filters. List view displays events sorted by distance with all relevant information. All filter state persists in URL for bookmarking.

Output: DiscoverEvents page, EventList component with React Query, EventCard display, EventFilters controls, LocationSearch and KeywordSearch components, and route registered in App.tsx.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-event-discovery/04-RESEARCH.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@apps/web/src/App.tsx
@apps/web/src/pages/Dashboard.tsx
@apps/web/src/api/events.api.ts
@apps/web/src/hooks/useGeolocation.ts
@apps/web/src/hooks/useEventFilters.ts
@packages/types/src/event.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create LocationSearch component with geolocation support</name>
  <files>apps/web/src/components/search/LocationSearch.tsx</files>
  <action>
Create LocationSearch component that:

1. Uses useGeolocation hook for "Use my location" button
2. Uses useEventFilters to update lat/lng in URL
3. Has text input for city name (geocoding deferred to Phase 5 per research open questions)
4. Shows loading state while getting location
5. Shows error toast if geolocation fails
6. Displays current location (if set) with distance radius selector

UI structure:
- "Use My Location" button (primary action)
- OR text: "Or enter coordinates manually:"
- Lat input (number)
- Lng input (number)
- Radius slider/select (10km, 25km, 50km, 100km, 200km options)
- Clear button to reset location

Use shadcn/ui components: Button, Input, Select from @/components/ui.

Import useToast for error notifications.

On geolocation success or manual entry, call updateFilter('lat', ...), updateFilter('lng', ...), updateFilter('radius', ...).
  </action>
  <verify>
File contains:
- LocationSearch component exported
- useGeolocation hook usage
- useEventFilters hook usage
- Button for geolocation with loading state
- Manual lat/lng inputs
- Radius selector (dropdown or slider)
- Error handling with toast
- Updates URL params on location change

Run: pnpm --filter @jamia/web build
Ensure no TypeScript errors.
  </verify>
  <done>
LocationSearch component created with geolocation button, manual coordinate entry, radius selection, and URL state integration.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create EventFilters and KeywordSearch components</name>
  <files>apps/web/src/components/events/EventFilters.tsx, apps/web/src/components/search/KeywordSearch.tsx</files>
  <action>
Create EventFilters component:
- Uses useEventFilters hook
- Event type checkboxes (jam, class, workshop, convention) - multi-select
- Date range picker using shadcn/ui Calendar or native date inputs
- "Clear Filters" button
- Follow Pattern from RESEARCH.md "Multi-Select Filter Component"

Create KeywordSearch component:
- Uses useEventFilters hook
- Text input for search query
- Uses useDebouncedCallback from use-debounce with 300ms delay
- Placeholder: "Search events..."
- Follow Pattern from RESEARCH.md "Debounced Search Input"

Both components should:
- Use shadcn/ui components (Input, Checkbox, Label, Button)
- Call updateFilter from useEventFilters on changes
- Display current filter values from filters object
- Follow existing component patterns from apps/web/src/components/

Import EVENT_TYPES constant:
```typescript
const EVENT_TYPES = [
  { value: 'jam', label: 'Jam' },
  { value: 'class', label: 'Class' },
  { value: 'workshop', label: 'Workshop' },
  { value: 'convention', label: 'Convention' },
] as const;
```
  </action>
  <verify>
EventFilters.tsx contains:
- Multi-select checkboxes for event types
- Date range picker (from/to)
- Clear filters button
- useEventFilters hook usage
- Updates URL on changes

KeywordSearch.tsx contains:
- Input field for search
- useDebouncedCallback with 300ms delay
- useEventFilters hook usage
- defaultValue from filters.query

Run: pnpm --filter @jamia/web build
Ensure no TypeScript errors.
  </verify>
  <done>
EventFilters and KeywordSearch components created with proper debouncing, URL state management, and multi-select support.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create EventCard and EventList components</name>
  <files>apps/web/src/components/events/EventCard.tsx, apps/web/src/components/events/EventList.tsx</files>
  <action>
Create EventCard component:
- Accepts event prop: Event & { distance_meters: number }
- Displays: title, type badge, location_text, starts_at (formatted), distance (converted to km)
- Distance format: "X km away" or "< 1 km away"
- Shows description (truncated to 150 chars with "...")
- Shows price if present
- Click/link to event detail page (will be created in plan 04-05)
- Use shadcn/ui Card component
- Follow existing card patterns from Dashboard.tsx

Create EventList component:
- Accepts no props (reads from URL via useEventFilters)
- Uses React Query useQuery to call eventsApi.searchEvents
- Shows loading skeleton while fetching
- Shows "No events found" if empty results
- Shows error toast if query fails
- Maps events to EventCard components
- Sorted by distance (backend already returns sorted)

React Query setup:
```typescript
const { filters } = useEventFilters();
const { data: events, isLoading, error } = useQuery({
  queryKey: ['events', 'search', filters],
  queryFn: () => eventsApi.searchEvents({
    lng: parseFloat(filters.lng),
    lat: parseFloat(filters.lat),
    radius: parseInt(filters.radius),
    types: filters.types,
    dateFrom: filters.dateFrom || undefined,
    dateTo: filters.dateTo || undefined,
    keyword: filters.query || undefined,
  }),
  enabled: !!filters.lat && !!filters.lng, // Only run if location set
});
```

Show message "Set your location to discover events" if !filters.lat or !filters.lng.
  </action>
  <verify>
EventCard.tsx contains:
- Event & distance_meters prop type
- Card UI with all event fields
- Distance formatted as km
- Type badge/label
- Date formatted
- Link/click handler for detail view

EventList.tsx contains:
- useEventFilters hook
- useQuery with eventsApi.searchEvents
- Loading state (skeleton)
- Empty state message
- Error handling
- Maps events to EventCard
- enabled: !!filters.lat && !!filters.lng

Run: pnpm --filter @jamia/web build
Ensure no TypeScript errors.
  </verify>
  <done>
EventCard and EventList components created with React Query integration, distance display, and proper loading/error states.
  </done>
</task>

<task type="auto">
  <name>Task 4: Create DiscoverEvents page and add route</name>
  <files>apps/web/src/pages/DiscoverEvents.tsx, apps/web/src/App.tsx</files>
  <action>
Create DiscoverEvents page:
- Layout: 2-column grid (filters sidebar + main content) on desktop, stacked on mobile
- Left sidebar: LocationSearch, KeywordSearch, EventFilters
- Main content: EventList
- Page title: "Discover Events"
- Use existing layout patterns from Dashboard.tsx

Composition:
```tsx
<div className="container mx-auto p-6">
  <h1 className="text-3xl font-bold mb-6">Discover Events</h1>
  <div className="grid grid-cols-1 lg:grid-cols-4 gap-6">
    <aside className="lg:col-span-1 space-y-4">
      <LocationSearch />
      <KeywordSearch />
      <EventFilters />
    </aside>
    <main className="lg:col-span-3">
      <EventList />
    </main>
  </div>
</div>
```

Add route to App.tsx:
- Path: /discover
- Element: <DiscoverEvents />
- No authentication required (public event discovery per requirements)
- Add to routes array alongside existing routes

Import DiscoverEvents at top of App.tsx.
  </action>
  <verify>
DiscoverEvents.tsx contains:
- Page layout with sidebar and main content
- LocationSearch, KeywordSearch, EventFilters in sidebar
- EventList in main area
- Responsive grid layout
- Page title

App.tsx contains:
- Import for DiscoverEvents
- Route definition for /discover
- Public route (no auth guard)

Run: pnpm --filter @jamia/web dev
Navigate to http://localhost:5173/discover
Page loads without errors (even if no events, UI renders).
  </verify>
  <done>
DiscoverEvents page created with full search and filter UI, and route registered for public access at /discover.
  </done>
</task>

</tasks>

<verification>
- All components follow shadcn/ui and existing codebase patterns
- LocationSearch integrates geolocation and manual entry
- EventFilters provides multi-select type filtering and date range
- KeywordSearch debounces input to reduce API calls
- EventCard displays all event info including distance
- EventList uses React Query with proper loading/error states
- DiscoverEvents page composes all components into cohesive UI
- Route registered and accessible at /discover
- TypeScript compilation succeeds
- No authentication required (public discovery)
</verification>

<success_criteria>
- User can navigate to /discover and see event discovery UI
- "Use My Location" button triggers browser geolocation
- Manual lat/lng entry updates URL and triggers search
- Event type checkboxes filter results (multi-select works)
- Date range filtering works (events within range shown)
- Keyword search filters by title/description with debouncing
- Events display in list sorted by distance (closest first)
- Distance shown in km for each event
- URL reflects all filter state (bookmarkable)
- Loading states show while fetching
- Empty state shows "No events found" when no results
</success_criteria>

<output>
After completion, create `.planning/phases/04-event-discovery/04-04-SUMMARY.md` with:
- Component hierarchy and composition
- React Query integration approach
- Filter state management (URL-based)
- Debouncing implementation
- Distance display formatting
- Route path and access level
</output>
