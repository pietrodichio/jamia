---
phase: 04-event-discovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/supabase/migrations/20260121100000_add_postgis_and_search.sql
autonomous: true

must_haves:
  truths:
    - "Events can be queried by distance from a point"
    - "Events can be filtered by search radius"
    - "Events can be found by keyword in title or description"
    - "Distance calculations are accurate (spherical)"
  artifacts:
    - path: "apps/web/supabase/migrations/20260121100000_add_postgis_and_search.sql"
      provides: "PostGIS extension, geography column, spatial index, full-text search"
      min_lines: 80
      contains: "CREATE EXTENSION postgis"
  key_links:
    - from: "events.location_geo"
      to: "PostGIS ST_Distance function"
      via: "RPC function search_events_by_location"
      pattern: "ST_Distance\\(.*location_geo"
    - from: "events.search_vector"
      to: "PostgreSQL full-text search"
      via: "tsvector GIN index"
      pattern: "search_vector @@ plainto_tsquery"
---

<objective>
Enable geospatial and full-text search capabilities for events discovery.

Purpose: Provide the database foundation for location-based event search with distance calculations and keyword filtering. PostGIS geography type ensures accurate spherical distance calculations, while full-text search with tsvector enables fast keyword queries.

Output: Migration file with PostGIS extension, geography column with GIST index, tsvector column with GIN index, and RPC function for distance-based searches.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-event-discovery/04-RESEARCH.md
@.planning/phases/03-event-foundation/03-01-SUMMARY.md
@apps/web/supabase/migrations/20260121000000_create_events_tables.sql
@packages/types/src/event.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create migration enabling PostGIS and adding geography column</name>
  <files>apps/web/supabase/migrations/20260121100000_add_postgis_and_search.sql</files>
  <action>
Create migration file that:

1. Enable PostGIS extension (required for geography type and spatial functions)
2. Add location_geo geography(POINT, 4326) column to events table
3. Populate location_geo from existing location_lat/location_lng using ST_SetSRID and ST_MakePoint
4. Create GIST index on location_geo for spatial query performance
5. Add constraint to ensure location_geo is set when location_lat/location_lng exist

Use geography type (not geometry) for accurate spherical calculations per research recommendations. SRID 4326 is WGS84 (standard for lat/lng coordinates).

Reference Pattern 1 from RESEARCH.md for exact PostGIS syntax.
  </action>
  <verify>
Migration file exists and contains:
- CREATE EXTENSION IF NOT EXISTS postgis
- ALTER TABLE events ADD COLUMN location_geo geography(POINT, 4326)
- UPDATE events SET location_geo = ... (population logic)
- CREATE INDEX idx_events_location_geo ON events USING GIST (location_geo)

File is valid SQL (no syntax errors).
  </verify>
  <done>
Migration file created with PostGIS extension enabled, geography column added, spatial index created, and existing coordinate data migrated to geography format.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add full-text search with tsvector column and GIN index</name>
  <files>apps/web/supabase/migrations/20260121100000_add_postgis_and_search.sql</files>
  <action>
Append to migration file:

1. Add search_vector tsvector GENERATED column combining title and description
2. Use to_tsvector('english', ...) with coalesce for null handling
3. Create GIN index on search_vector for fast full-text queries
4. Use GENERATED ALWAYS AS ... STORED for automatic updates

This enables sub-5ms keyword searches per research benchmarks. GIN index is essential for tsvector query performance.

Reference Pattern 4 from RESEARCH.md for exact PostgreSQL full-text search syntax.

DO NOT use trigger-based approach - generated column is simpler and self-maintaining.
  </action>
  <verify>
Migration file contains:
- ALTER TABLE events ADD COLUMN search_vector tsvector GENERATED ALWAYS AS (...)
- CREATE INDEX idx_events_search_vector ON events USING GIN (search_vector)

Generated column references title and description fields.
  </verify>
  <done>
Full-text search enabled with automatically-updated tsvector column and GIN index for fast keyword queries.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create RPC function for geospatial search with filters</name>
  <files>apps/web/supabase/migrations/20260121100000_add_postgis_and_search.sql</files>
  <action>
Append to migration file:

Create function search_events_by_location that:
- Accepts: search_lng, search_lat, radius_meters (default 50000), event_types (text[]), date_from, date_to, keyword (all optional)
- Returns: events with calculated distance_meters
- Uses ST_DWithin for radius filtering (leverages spatial index)
- Uses ST_Distance for distance calculation (geography ensures accuracy)
- Filters by status = 'published' always
- Filters by type IN (event_types) if provided
- Filters by starts_at >= date_from if provided
- Filters by starts_at <= date_to if provided
- Filters by search_vector @@ plainto_tsquery if keyword provided
- Orders by distance_meters ASC

Use LANGUAGE plpgsql STABLE for query planning optimization. SECURITY DEFINER not needed (no RLS bypass required).

Reference Pattern 2 from RESEARCH.md for RPC function structure.

IMPORTANT: Use ST_DWithin for filtering BEFORE ST_Distance for sorting - this uses the spatial index efficiently.
  </action>
  <verify>
Migration file contains:
- CREATE OR REPLACE FUNCTION search_events_by_location(...)
- ST_DWithin used in WHERE clause for radius filtering
- ST_Distance used in SELECT for distance calculation
- Filters for status, type, dates, keyword included
- ORDER BY distance_meters ASC

Function signature matches expected parameters.
  </verify>
  <done>
RPC function created that performs efficient geospatial search with distance sorting and multiple filter options, leveraging spatial and full-text indexes.
  </done>
</task>

</tasks>

<verification>
- Migration file is valid SQL with no syntax errors
- PostGIS extension enabled with IF NOT EXISTS (safe for re-run)
- Geography column uses POINT geometry type with SRID 4326
- GIST index created on geography column
- GIN index created on tsvector column
- RPC function signature matches expected API contract
- ST_DWithin used for filtering, ST_Distance for sorting (correct order for index usage)
</verification>

<success_criteria>
- Migration file creates all required database objects (extension, columns, indexes, function)
- Existing location data migrated to geography format
- Spatial queries can use GIST index (ST_DWithin returns results efficiently)
- Full-text queries can use GIN index (tsvector searches are fast)
- RPC function returns events sorted by distance with optional filters
- No data loss during migration (coordinates preserved in geography column)
</success_criteria>

<output>
After completion, create `.planning/phases/04-event-discovery/04-01-SUMMARY.md` with:
- Migration file path and key additions
- PostGIS pattern used (geography vs geometry choice)
- Index creation for performance
- RPC function signature and filtering logic
- Any deviations from research recommendations
</output>
