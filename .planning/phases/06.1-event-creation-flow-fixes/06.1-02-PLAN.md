---
phase: 06.1-event-creation-flow-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/components/forms/wizard/WizardStepIndicator.tsx
  - apps/web/src/components/forms/event/EventPreviewStep.tsx
autonomous: true

must_haves:
  truths:
    - "WizardStepIndicator displays consistently on mobile and desktop"
    - "EventPreviewStep handles undefined/null values without showing 'undefined' text"
    - "Preview shows meaningful fallbacks for missing data"
  artifacts:
    - path: "apps/web/src/components/forms/wizard/WizardStepIndicator.tsx"
      provides: "Responsive step indicator with consistent spacing"
      min_lines: 60
    - path: "apps/web/src/components/forms/event/EventPreviewStep.tsx"
      provides: "Null-safe preview with fallback messages"
      min_lines: 100
  key_links:
    - from: "EventPreviewStep"
      to: "date-fns format"
      via: "formatDateTime helper with try/catch"
      pattern: "format\\(.*locale: it"
    - from: "EventPreviewStep"
      to: "DOMPurify"
      via: "sanitize rich text HTML"
      pattern: "DOMPurify\\.sanitize"
---

<objective>
Fix WizardStepIndicator layout consistency and EventPreviewStep null-safety to eliminate visual bugs and "undefined" text displays.

Purpose: Ensure wizard navigation looks professional on all screen sizes and event preview gracefully handles incomplete form data without breaking or showing literal "undefined" text.

Output: Polished wizard UI with responsive step indicator and robust preview component that shows meaningful placeholders for missing values.
</objective>

<execution_context>
@/Users/pietrodichio/development/personal/jamia/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pietrodichio/development/personal/jamia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/pietrodichio/development/personal/jamia/.planning/PROJECT.md
@/Users/pietrodichio/development/personal/jamia/.planning/ROADMAP.md
@/Users/pietrodichio/development/personal/jamia/.planning/STATE.md
@/Users/pietrodichio/development/personal/jamia/.planning/phases/06.1-event-creation-flow-fixes/06.1-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Fix WizardStepIndicator spacing and layout</name>
  <files>apps/web/src/components/forms/wizard/WizardStepIndicator.tsx</files>
  <action>
    Review and fix WizardStepIndicator.tsx spacing consistency on mobile and desktop.

    Issues to address:
    - Verify flex container spacing is consistent (gap-2 or gap-4)
    - Ensure separator lines (flex-1 borders) align properly between step circles
    - Confirm mobile responsive classes hide labels correctly (use hidden md:block pattern)
    - Check step circle sizes are consistent (w-8 h-8 or w-10 h-10)
    - Verify text truncation for long step labels

    According to RESEARCH.md (line 348), current implementation (lines 48-63) already has mobile/desktop pattern.
    Verify this is working correctly and spacing is visually consistent.

    If layout is already correct, document verification and ensure no regressions.
    If issues exist, apply responsive classes and consistent gap spacing.
  </action>
  <verify>
    Visual inspection: Component renders without layout breaks.
    Build passes: `pnpm --filter @jamia/web build`
  </verify>
  <done>
    WizardStepIndicator renders with consistent spacing on mobile (320px) and desktop (1920px), separators align between circles, labels show/hide responsively.
  </done>
</task>

<task type="auto">
  <name>Fix EventPreviewStep null handling</name>
  <files>apps/web/src/components/forms/event/EventPreviewStep.tsx</files>
  <action>
    Refactor EventPreviewStep.tsx to handle undefined/null values gracefully (RESEARCH.md lines 283-315).

    Current issues (RESEARCH.md line 285):
    - Lines 20-28, 32-42 don't handle null dates properly
    - Template literals show "undefined" text
    - Missing values break format() calls

    Fixes to implement:

    1. Create formatDateTime helper:
    ```typescript
    const formatDateTime = (date?: Date, time?: string) => {
      if (!date) return 'Non specificata';
      try {
        const dateStr = format(date, 'dd MMMM yyyy', { locale: it });
        if (time) return `${dateStr}, ${time}`;
        return dateStr;
      } catch (error) {
        return 'Data non valida';
      }
    };
    ```

    2. Add null-safe price display:
    ```typescript
    {formValues.price && formValues.price > 0
      ? `€${formValues.price.toFixed(2)}`
      : 'Gratuito'}
    ```

    3. Add placeholder for missing description:
    ```typescript
    {formValues.description ? (
      <div dangerouslySetInnerHTML={{ __html: DOMPurify.sanitize(formValues.description) }} />
    ) : (
      <p className="text-muted-foreground italic">Nessuna descrizione fornita</p>
    )}
    ```

    4. Add DOMPurify import and sanitization for rich text HTML
    5. Use optional chaining for all formValues access
    6. Show "Non specificato" for missing optional fields (link, tags, etc.)

    Import DOMPurify: `import DOMPurify from 'dompurify';`
    Apply null coalescing and fallbacks to all displayed values.
  </action>
  <verify>
    Build passes: `pnpm --filter @jamia/web build`
    Preview shows fallback text instead of "undefined" for missing values.
  </verify>
  <done>
    EventPreviewStep handles all undefined/null values with meaningful fallbacks, sanitizes rich text HTML with DOMPurify, shows Italian placeholder messages for missing data.
  </done>
</task>

</tasks>

<verification>
WizardStepIndicator layout is consistent across screen sizes.
EventPreviewStep displays gracefully with incomplete form data.
No "undefined" or "null" text visible in preview.
</verification>

<success_criteria>
- [ ] WizardStepIndicator verified for responsive layout consistency
- [ ] EventPreviewStep refactored with formatDateTime helper
- [ ] DOMPurify added for HTML sanitization
- [ ] All preview fields show meaningful fallbacks (not "undefined")
- [ ] Build passes: `pnpm --filter @jamia/web build`
- [ ] Italian placeholder messages used throughout
</success_criteria>

<output>
After completion, create `.planning/phases/06.1-event-creation-flow-fixes/06.1-02-SUMMARY.md`
</output>
