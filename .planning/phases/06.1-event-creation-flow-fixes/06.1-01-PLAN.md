---
phase: 06.1-event-creation-flow-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/components/jams/DescriptionEditor.tsx
  - apps/web/src/components/jams/LocationInput.tsx
  - apps/web/src/components/jams/DateTimePicker.tsx
autonomous: true

must_haves:
  truths:
    - "DescriptionEditor component renders react-quill with emoji support"
    - "LocationInput component fetches Google Maps predictions with debounce"
    - "DateTimePicker component combines Calendar popover with time input"
  artifacts:
    - path: "apps/web/src/components/jams/DescriptionEditor.tsx"
      provides: "Reusable rich text editor with toolbar and emoji picker"
      min_lines: 60
    - path: "apps/web/src/components/jams/LocationInput.tsx"
      provides: "Google Maps autocomplete with session token management"
      min_lines: 120
    - path: "apps/web/src/components/jams/DateTimePicker.tsx"
      provides: "Combined date/time picker with Italian locale"
      min_lines: 80
  key_links:
    - from: "DescriptionEditor"
      to: "react-quill"
      via: "ReactQuill component import"
      pattern: "import ReactQuill from"
    - from: "LocationInput"
      to: "google.maps.places.AutocompleteSuggestion"
      via: "fetchAutocompleteSuggestions API call"
      pattern: "AutocompleteSuggestion\\.fetchAutocompleteSuggestions"
    - from: "DateTimePicker"
      to: "Calendar component"
      via: "Popover with Calendar"
      pattern: "import.*Calendar.*from"
---

<objective>
Extract reusable form components from JamForm.tsx into standalone, reusable components for the event wizard.

Purpose: Create DescriptionEditor (rich text with emoji), LocationInput (Google Maps autocomplete), and DateTimePicker (Calendar + time) as reusable components that both JamForm and EventWizard can use without duplication.

Output: Three new components in apps/web/src/components/jams/ following established patterns from JamForm.tsx with proper Italian localization and error handling.
</objective>

<execution_context>
@/Users/pietrodichio/development/personal/jamia/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pietrodichio/development/personal/jamia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/pietrodichio/development/personal/jamia/.planning/PROJECT.md
@/Users/pietrodichio/development/personal/jamia/.planning/ROADMAP.md
@/Users/pietrodichio/development/personal/jamia/.planning/STATE.md
@/Users/pietrodichio/development/personal/jamia/.planning/phases/06.1-event-creation-flow-fixes/06.1-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Extract DescriptionEditor component</name>
  <files>apps/web/src/components/jams/DescriptionEditor.tsx</files>
  <action>
    Create DescriptionEditor.tsx extracting the react-quill implementation from JamForm.tsx.

    Component interface:
    - Props: { value: string; onChange: (value: string) => void; disabled?: boolean; placeholder?: string }
    - Imports: react-quill, quill-emoji with CSS imports
    - Toolbar config: headers [1, 2], bold/italic/underline/strike, lists, link, emoji, clean
    - Modules: emoji-toolbar, emoji-textarea, emoji-shortname as per JamForm pattern
    - Default placeholder: "Descrivi l'evento..." (Italian)
    - Theme: "snow"

    Reference JamForm.tsx for exact toolbar configuration and module setup.
    Use Italian placeholder text for consistency with app localization.
    Export as named export for clarity.
  </action>
  <verify>
    Verify component compiles without errors:
    `pnpm --filter @jamia/web build`
  </verify>
  <done>
    DescriptionEditor.tsx exists, exports DescriptionEditor component, imports react-quill and quill-emoji with proper CSS, has toolbar with emoji support.
  </done>
</task>

<task type="auto">
  <name>Extract LocationInput component</name>
  <files>apps/web/src/components/jams/LocationInput.tsx</files>
  <action>
    Create LocationInput.tsx extracting Google Maps autocomplete logic from JamForm.tsx (lines 287-354).

    Component interface:
    - Props: { value: string; onChange: (location: { description: string; latitude?: number; longitude?: number; googleMapsUrl?: string }) => void; disabled?: boolean }
    - State: placePredictions, isLoadingPlaces, showDropdown
    - Debounce: 350ms for predictions fetch (only when input.length >= 3)
    - Session token: Create new AutocompleteSessionToken per autocomplete series
    - API: Use google.maps.places.AutocompleteSuggestion.fetchAutocompleteSuggestions (new API format)
    - On selection: call place.fetchFields with displayName, formattedAddress, location, id, googleMapsURI
    - Extract: lat/lng from place.location, description from displayName || formattedAddress
    - UI: Input with dropdown showing up to 5 predictions, loading state, error handling
    - Google Maps script loading: Use existing loadGoogleMaps utility pattern from JamForm

    Reference JamForm.tsx lines 302-346 for exact implementation.
    Include proper error handling for API failures.
    Show "Nessun risultato" when predictions array is empty after fetch.
  </action>
  <verify>
    Verify component compiles and Google Maps types are available:
    `pnpm --filter @jamia/web build`
  </verify>
  <done>
    LocationInput.tsx exists, implements debounced autocomplete, uses AutocompleteSuggestion API, handles place selection with lat/lng extraction, manages session tokens correctly.
  </done>
</task>

<task type="auto">
  <name>Extract DateTimePicker component</name>
  <files>apps/web/src/components/jams/DateTimePicker.tsx</files>
  <action>
    Create DateTimePicker.tsx extracting date/time picker pattern from JamForm.tsx.

    Component interface:
    - Props: { date?: Date; time: string; onDateChange: (date: Date | undefined) => void; onTimeChange: (time: string) => void; minDate?: Date; disabled?: boolean }
    - UI: Popover trigger button (shows formatted date or "Seleziona una data") + Calendar component + Time input (type="time")
    - Layout: Flex container with gap-4, calendar gets flex-1, time input fixed width
    - Calendar: mode="single", Italian locale (it from date-fns), disabled prop for minDate constraint
    - Date formatting: Use formatDateForDisplay(date, "it-IT") pattern from JamForm
    - Popover: close on date selection for better UX
    - Time input: className="rounded-xl" for consistency

    Reference JamForm.tsx DateTimePicker implementation.
    Import Calendar from @/components/ui/calendar and Popover components.
    Use Italian locale for all date formatting.
  </action>
  <verify>
    Verify component compiles and shadcn components are available:
    `pnpm --filter @jamia/web build`
  </verify>
  <done>
    DateTimePicker.tsx exists, combines Calendar popover with time input, uses Italian locale for date display, supports minDate constraint, has consistent styling.
  </done>
</task>

</tasks>

<verification>
All three components compile successfully and can be imported from apps/web/src/components/jams/.
Components follow established patterns from JamForm.tsx.
Italian localization is applied consistently.
</verification>

<success_criteria>
- [ ] DescriptionEditor.tsx created with react-quill + emoji toolbar
- [ ] LocationInput.tsx created with Google Maps autocomplete and debounce
- [ ] DateTimePicker.tsx created with Calendar popover + time input
- [ ] All components use Italian locale and placeholders
- [ ] Build passes: `pnpm --filter @jamia/web build`
- [ ] Components are exported and ready for reuse in EventWizard
</success_criteria>

<output>
After completion, create `.planning/phases/06.1-event-creation-flow-fixes/06.1-01-SUMMARY.md`
</output>
