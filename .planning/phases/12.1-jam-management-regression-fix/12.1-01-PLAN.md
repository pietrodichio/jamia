---
phase: 12.1-jam-management-regression-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/supabase/migrations/YYYYMMDDHHMMSS_add_source_event_id_and_jam_fields.sql
  - packages/types/src/event.ts
  - apps/backend/src/events/dto/create-event.dto.ts
  - apps/backend/src/events/dto/save-draft-event.dto.ts
autonomous: true

must_haves:
  truths:
    - "jams table has source_event_id column linking back to events"
    - "events table has jam management columns (manage_participants, capacity, desired_bases_min/max, desired_flyers_min/max, auto_promote, public_participants)"
    - "CreateEventDto accepts jam management fields for type='jam' events"
  artifacts:
    - path: "apps/web/supabase/migrations/YYYYMMDDHHMMSS_add_source_event_id_and_jam_fields.sql"
      provides: "Database schema for bidirectional sync"
      contains: "source_event_id"
    - path: "packages/types/src/event.ts"
      provides: "Shared types with jam management fields"
      contains: "manage_participants"
    - path: "apps/backend/src/events/dto/create-event.dto.ts"
      provides: "Backend DTO validation for jam fields"
      contains: "manage_participants"
  key_links:
    - from: "events.source_jam_id"
      to: "jams.source_event_id"
      via: "bidirectional FK relationship"
      pattern: "source_event_id.*REFERENCES.*events"
---

<objective>
Add database columns and DTO fields to support bidirectional jam-event sync.

Purpose: Enable reverse sync (event -> jam) by adding source_event_id to jams table and jam management columns to events table. This is foundational infrastructure for the fix.

Output: Migration file, updated shared types, updated backend DTOs
</objective>

<execution_context>
@/Users/pietrodichio/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pietrodichio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12.1-jam-management-regression-fix/12.1-RESEARCH.md
@apps/web/supabase/migrations/20260121120300_add_jam_sync_column.sql
@apps/web/supabase/migrations/20250101000000_base_schema.sql
@packages/types/src/event.ts
@apps/backend/src/events/dto/create-event.dto.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create migration for source_event_id and jam management columns</name>
  <files>apps/web/supabase/migrations/YYYYMMDDHHMMSS_add_source_event_id_and_jam_fields.sql</files>
  <action>
Create a new migration file (use current timestamp format YYYYMMDDHHMMSS):

1. Add `source_event_id` column to `jams` table:
   - Type: UUID
   - References events(id) ON DELETE CASCADE
   - Create partial index on source_event_id WHERE NOT NULL
   - Create unique partial index (one jam per event)
   - Add comment explaining the purpose

2. Add jam management columns to `events` table:
   - manage_participants: BOOLEAN DEFAULT FALSE
   - capacity: INTEGER (nullable)
   - desired_bases_min: INTEGER (nullable)
   - desired_bases_max: INTEGER (nullable)
   - desired_flyers_min: INTEGER (nullable)
   - desired_flyers_max: INTEGER (nullable)
   - auto_promote: BOOLEAN DEFAULT TRUE
   - public_participants: BOOLEAN DEFAULT TRUE
   - Add comments for clarity

Note: These columns mirror the jams table columns. Only populated when type='jam' AND manage_participants=true.

Pattern follows existing migration 20260121120300_add_jam_sync_column.sql which added source_jam_id to events.
  </action>
  <verify>
cd apps/web && npx supabase db reset --yes
# Check columns exist:
# psql "postgresql://postgres:postgres@127.0.0.1:54422/postgres" -c "\\d jams" | grep source_event_id
# psql "postgresql://postgres:postgres@127.0.0.1:54422/postgres" -c "\\d events" | grep manage_participants
  </verify>
  <done>Migration applies cleanly. source_event_id exists in jams table. Jam management columns exist in events table.</done>
</task>

<task type="auto">
  <name>Task 2: Update shared types with jam management fields</name>
  <files>packages/types/src/event.ts</files>
  <action>
Update packages/types/src/event.ts:

1. Add jam management fields to Event interface:
   - manage_participants?: boolean
   - capacity?: number | null
   - desired_bases_min?: number | null
   - desired_bases_max?: number | null
   - desired_flyers_min?: number | null
   - desired_flyers_max?: number | null
   - auto_promote?: boolean
   - public_participants?: boolean
   - source_event_id?: string | null (for linked jam lookup)

2. Add jam management fields to CreateEventDto interface:
   - manage_participants?: boolean (when true, creates corresponding jam record)
   - capacity?: number
   - desired_bases_min?: number
   - desired_bases_max?: number
   - desired_flyers_min?: number
   - desired_flyers_max?: number
   - auto_promote?: boolean
   - public_participants?: boolean

These fields are optional and only used when type='jam' and manage_participants=true.
  </action>
  <verify>cd /Users/pietrodichio/development/personal/jamia && pnpm --filter @jamia/types build</verify>
  <done>Shared types compile. Event interface includes jam management fields. CreateEventDto includes jam management fields.</done>
</task>

<task type="auto">
  <name>Task 3: Update backend DTOs with class-validator decorators</name>
  <files>apps/backend/src/events/dto/create-event.dto.ts, apps/backend/src/events/dto/save-draft-event.dto.ts</files>
  <action>
Update apps/backend/src/events/dto/create-event.dto.ts:

Add jam management fields with class-validator decorators:

```typescript
// Jam management fields (only used when type='jam' and managing participants)
@IsOptional()
@IsBoolean()
manage_participants?: boolean;

@IsOptional()
@Type(() => Number)
@IsNumber()
capacity?: number;

@IsOptional()
@Type(() => Number)
@IsNumber()
desired_bases_min?: number;

@IsOptional()
@Type(() => Number)
@IsNumber()
desired_bases_max?: number;

@IsOptional()
@Type(() => Number)
@IsNumber()
desired_flyers_min?: number;

@IsOptional()
@Type(() => Number)
@IsNumber()
desired_flyers_max?: number;

@IsOptional()
@IsBoolean()
auto_promote?: boolean;

@IsOptional()
@IsBoolean()
public_participants?: boolean;
```

Also update save-draft-event.dto.ts with the same fields (it extends/mirrors CreateEventDto pattern).

Import IsBoolean from class-validator if not already imported.
  </action>
  <verify>cd /Users/pietrodichio/development/personal/jamia && pnpm --filter backend build</verify>
  <done>Backend compiles. CreateEventDto and SaveDraftEventDto include jam management fields with proper decorators.</done>
</task>

</tasks>

<verification>
1. Run `cd apps/web && npx supabase db reset --yes` - migration applies cleanly
2. Run `pnpm build` from root - all packages compile
3. Query database to confirm columns exist:
   - jams.source_event_id column present
   - events has manage_participants, capacity, desired_bases_min/max, desired_flyers_min/max, auto_promote, public_participants columns
</verification>

<success_criteria>
- Database schema supports bidirectional sync (source_jam_id in events, source_event_id in jams)
- Events table has all jam management columns for storing jam-specific data
- Shared types include jam management fields
- Backend DTOs validate jam management fields
- All builds pass
</success_criteria>

<output>
After completion, create `.planning/phases/12.1-jam-management-regression-fix/12.1-01-SUMMARY.md`
</output>
