---
phase: 12.1-jam-management-regression-fix
plan: 01
subsystem: database
tags: [supabase, postgresql, typescript, nestjs, dto, bidirectional-sync]

# Dependency graph
requires:
  - phase: 03-event-foundation
    provides: events table, source_jam_id column
  - phase: base
    provides: jams table
provides:
  - source_event_id column for jams table (reverse sync link)
  - jam management columns on events table (manage_participants, capacity, desired_bases_min/max, desired_flyers_min/max, auto_promote, public_participants)
  - shared types for jam management fields
  - backend DTO validation for jam management fields
affects: [12.1-02, 12.1-03, events-service, jams-service]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - Bidirectional FK pattern (source_jam_id on events, source_event_id on jams)
    - Partial unique indexes for one-to-one relationships

key-files:
  created:
    - apps/web/supabase/migrations/20260224154021_add_source_event_id_and_jam_fields.sql
  modified:
    - packages/types/src/event.ts
    - apps/backend/src/events/dto/create-event.dto.ts

key-decisions:
  - "Add jam management columns directly to events table (not JSONB) for better queryability and sync simplicity"
  - "Use partial unique index for one-to-one relationship between events and jams"

patterns-established:
  - "Bidirectional FK pattern: source_jam_id (events->jams) + source_event_id (jams->events)"

# Metrics
duration: 5min
completed: 2026-02-24
---

# Phase 12.1 Plan 01: Database Schema Summary

**Database schema with source_event_id on jams table and jam management columns on events table for bidirectional sync**

## Performance

- **Duration:** 5 min
- **Started:** 2026-02-24T14:40:14Z
- **Completed:** 2026-02-24T14:45:07Z
- **Tasks:** 3
- **Files modified:** 3

## Accomplishments
- Added source_event_id column to jams table for reverse sync (event -> jam)
- Added jam management columns to events table (manage_participants, capacity, desired_bases_min/max, desired_flyers_min/max, auto_promote, public_participants)
- Updated shared types with jam management fields for both Event and CreateEventDto interfaces
- Updated backend CreateEventDto with class-validator decorators for jam management fields

## Task Commits

Each task was committed atomically:

1. **Task 1: Create migration for source_event_id and jam management columns** - `3ae610c` (feat)
2. **Task 2: Update shared types with jam management fields** - `b98c1ce` (feat)
3. **Task 3: Update backend DTOs with class-validator decorators** - `1d29eab` (feat)

## Files Created/Modified
- `apps/web/supabase/migrations/20260224154021_add_source_event_id_and_jam_fields.sql` - Migration adding source_event_id to jams and jam management columns to events
- `packages/types/src/event.ts` - Shared types with jam management fields for Event and CreateEventDto interfaces
- `apps/backend/src/events/dto/create-event.dto.ts` - Backend DTO with class-validator decorators for jam management fields

## Decisions Made
- Added jam management columns directly to events table rather than using JSONB - enables SQL queries and joins
- SaveDraftEventDto automatically inherits jam management fields via PartialType(CreateEventDto) - no duplicate code needed

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- Database schema complete for bidirectional sync
- Shared types ready for frontend and backend consumption
- Backend DTOs ready to accept jam management fields
- Ready for Plan 02: EventsService reverse sync implementation

---
*Phase: 12.1-jam-management-regression-fix*
*Completed: 2026-02-24*
