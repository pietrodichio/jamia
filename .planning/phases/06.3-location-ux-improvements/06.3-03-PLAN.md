---
phase: 06.3-location-ux-improvements
plan: 03
type: execute
wave: 2
depends_on: ["06.3-01", "06.3-02"]
files_modified:
  - apps/web/src/components/events/EventCardGrid.tsx
  - apps/web/src/components/events/UnifiedEventView.tsx
autonomous: true

must_haves:
  truths:
    - "Empty state explains WHY no events are shown (no location vs no results)"
    - "Empty state provides actionable CTAs (set location, expand radius, try city search)"
    - "Loading state shows skeleton cards (not just text)"
    - "Error state offers retry action"
  artifacts:
    - path: "apps/web/src/components/events/EventCardGrid.tsx"
      provides: "Context-aware empty state with guidance"
      contains: "Imposta la tua posizione"
    - path: "apps/web/src/components/events/UnifiedEventView.tsx"
      provides: "Pass empty state context to EventCardGrid"
      contains: "emptyStateContext"
  key_links:
    - from: "apps/web/src/components/events/EventCardGrid.tsx"
      to: "Empty state UI"
      via: "Conditional rendering based on context"
      pattern: "hasLocation.*emptyStateContext"
    - from: "apps/web/src/components/events/UnifiedEventView.tsx"
      to: "EventCardGrid"
      via: "emptyStateContext prop"
      pattern: "emptyStateContext="
---

<objective>
Improve empty and loading states with clear guidance and actionable CTAs.

Purpose: Generic "Nessun evento trovato" provides no path forward. Users need to understand why (no location? no results in area?) and what to do (set location, expand radius, try different city).

Output: Context-aware empty states that guide users to successful discovery, plus skeleton loading states.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.3-location-ux-improvements/06.3-RESEARCH.md

@apps/web/src/components/events/EventCardGrid.tsx
@apps/web/src/components/events/UnifiedEventView.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create context-aware empty states in EventCardGrid</name>
  <files>apps/web/src/components/events/EventCardGrid.tsx</files>
  <action>
Replace generic empty state with context-aware guidance:

1. Add props interface update:
   ```typescript
   interface EventCardGridProps {
     events: Event[];
     emptyStateContext?: {
       hasLocation: boolean;
       radius?: string;
       searchQuery?: string;
       onSetLocation?: () => void;
       onExpandRadius?: () => void;
       onClearFilters?: () => void;
     };
   }
   ```

2. Create three empty state variations:

   **No Location Set:**
   - Icon: MapPin (lucide-react)
   - Title: "Imposta la tua posizione"
   - Description: "Per trovare eventi vicino a te, imposta la tua posizione o cerca una città."
   - Primary CTA button: "Rileva posizione" (calls onSetLocation)
   - Secondary text: "oppure cerca una città specifica"

   **Location Set, No Results:**
   - Icon: Search (lucide-react)
   - Title: "Nessun evento in questa zona"
   - Description: "Non ci sono eventi entro {radius} km. Prova ad espandere il raggio di ricerca."
   - Primary CTA button: "Espandi a {nextRadius} km" (e.g., 50->100, 100->200)
   - Secondary CTA: "Prova un'altra città"

   **Location Set, Filters Applied, No Results:**
   - Icon: FilterX (lucide-react)
   - Title: "Nessun evento con questi filtri"
   - Description: "Prova a rimuovere alcuni filtri per vedere più risultati."
   - Primary CTA button: "Rimuovi filtri" (calls onClearFilters)
   - Secondary text: Shows which filters are active

3. Style with Tailwind:
   - Center vertically and horizontally
   - Icon at 48px in muted color
   - Title in text-lg font-semibold
   - Description in text-muted-foreground
   - Button uses shadcn Button component
  </action>
  <verify>
- `cat apps/web/src/components/events/EventCardGrid.tsx` shows:
  - emptyStateContext prop in interface
  - Three conditional empty state variations
  - Button with onSetLocation callback
- TypeScript compiles: `cd apps/web && npx tsc --noEmit`
  </verify>
  <done>
EventCardGrid shows context-aware empty states with specific guidance based on whether location is set and whether filters are applied.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire empty state context from UnifiedEventView</name>
  <files>apps/web/src/components/events/UnifiedEventView.tsx</files>
  <action>
Pass empty state context to EventCardGrid:

1. Create emptyStateContext object in component:
   ```typescript
   const emptyStateContext = {
     hasLocation,
     radius: filters.radius,
     searchQuery: filters.query,
     onSetLocation: () => {
       // Trigger location detection
       // Could scroll to or highlight LocationSearch component
     },
     onExpandRadius: () => {
       const currentRadius = parseInt(filters.radius);
       const nextRadius = currentRadius < 100 ? 100 : currentRadius < 200 ? 200 : 500;
       updateFilter('radius', nextRadius.toString());
     },
     onClearFilters: clearFilters,
   };
   ```

2. Pass to EventCardGrid:
   `<EventCardGrid events={events || []} emptyStateContext={emptyStateContext} />`

3. Add skeleton loading state (replace text "Caricamento..."):
   - Create 3 skeleton cards using shadcn Skeleton component
   - Card skeleton: 300px height, rounded-lg, animate-pulse
   - Include skeleton for image area (top 60%), title (2 lines), meta (1 line)

4. Improve error state:
   - Add "Riprova" button that refetches query
   - Use React Query's refetch function
  </action>
  <verify>
- `cat apps/web/src/components/events/UnifiedEventView.tsx` shows:
  - emptyStateContext object with callbacks
  - EventCardGrid receiving emptyStateContext prop
  - Skeleton cards during loading
- Start dev server and visit /discover
- Clear all URL params (no location)
- See "Imposta la tua posizione" empty state
- Click "Rileva posizione" - location detection triggers
  </verify>
  <done>
UnifiedEventView passes empty state context enabling interactive empty states, shows skeleton loading, and offers error retry.
  </done>
</task>

</tasks>

<verification>
1. Visit /discover with no URL params (fresh)
2. If IP auto-detect hasn't completed yet, see "Imposta la tua posizione" with CTA
3. After location applied, if no events in area, see "Nessun evento in questa zona"
4. Click "Espandi a 100 km" - radius updates, search refetches
5. Apply type filter that has no results - see "Nessun evento con questi filtri"
6. Click "Rimuovi filtri" - filters clear, results appear
7. During loading (slow network), see skeleton cards instead of text
8. Force error (disconnect network) - see error state with "Riprova" button
</verification>

<success_criteria>
- Zero "dead end" states - every empty state has actionable next step
- Users understand WHY no events are shown
- Loading state is visually informative (skeletons)
- Error state offers recovery action
</success_criteria>

<output>
After completion, create `.planning/phases/06.3-location-ux-improvements/06.3-03-SUMMARY.md`
</output>
