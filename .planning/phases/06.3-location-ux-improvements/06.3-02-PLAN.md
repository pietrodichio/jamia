---
phase: 06.3-location-ux-improvements
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/components/search/CitySearchInput.tsx
  - apps/web/src/components/search/LocationSearch.tsx
autonomous: true

must_haves:
  truths:
    - "User can search for any city using autocomplete dropdown"
    - "Selecting a city applies its coordinates to the discovery filters"
    - "City search uses existing Google Maps API integration (no new API keys)"
    - "Search location is temporary (URL params only, does not modify profile)"
  artifacts:
    - path: "apps/web/src/components/search/CitySearchInput.tsx"
      provides: "City autocomplete input with Google Places API"
      exports: ["CitySearchInput"]
    - path: "apps/web/src/components/search/LocationSearch.tsx"
      provides: "Location search with city search integration"
      contains: "CitySearchInput"
  key_links:
    - from: "apps/web/src/components/search/CitySearchInput.tsx"
      to: "Google Places API"
      via: "AutocompleteSuggestion.fetchAutocompleteSuggestions"
      pattern: "fetchAutocompleteSuggestions"
    - from: "apps/web/src/components/search/CitySearchInput.tsx"
      to: "useGoogleMaps hook"
      via: "hook import"
      pattern: "useGoogleMaps"
---

<objective>
Add city search capability to location discovery for trip planning use case.

Purpose: Users planning trips need to explore events in other cities. City search reuses existing Google Maps integration from event creation.

Output: CitySearchInput component with cities-only autocomplete, integrated into LocationSearch as an expandable option.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.3-location-ux-improvements/06.3-RESEARCH.md

@apps/web/src/components/forms/event/EventTypeStep.tsx
@apps/web/src/hooks/useGoogleMaps.ts
@apps/web/src/components/search/LocationSearch.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CitySearchInput component</name>
  <files>apps/web/src/components/search/CitySearchInput.tsx</files>
  <action>
Create a new CitySearchInput component following EventTypeStep.tsx pattern:

1. Props interface:
   - onCitySelect: (location: { lat: number; lng: number; city: string }) => void
   - placeholder?: string (default: "Cerca una città...")
   - disabled?: boolean

2. Internal state:
   - searchText: string (input value)
   - predictions: array of place predictions
   - isLoading: boolean
   - isOpen: boolean (dropdown visibility)

3. Google Places integration (mirror EventTypeStep pattern):
   - Use useGoogleMaps(import.meta.env.VITE_GOOGLE_MAPS_API_KEY)
   - Create AutocompleteSessionToken for each search session
   - Use 350ms debounce (same as EventTypeStep)
   - Request config: { input: searchText, types: ['(cities)'], sessionToken }
   - Map suggestions to { description, place_id, placePrediction }

4. Selection handler:
   - Call placePrediction.toPlace() then fetchFields with ["location", "addressComponents"]
   - Extract lat/lng from location.lat() and location.lng()
   - Extract city name from addressComponents (locality or administrative_area_level_3)
   - Call onCitySelect({ lat, lng, city })
   - Clear input and close dropdown after selection

5. UI structure:
   - Input with search icon (lucide-react Search)
   - Dropdown below input showing predictions
   - Loading spinner during fetch
   - Keyboard navigation (up/down arrows, Enter to select, Escape to close)

6. Style with Tailwind classes matching existing shadcn/ui patterns.
  </action>
  <verify>
- `cat apps/web/src/components/search/CitySearchInput.tsx` shows:
  - useGoogleMaps hook usage
  - types: ['(cities)'] in request config
  - 350ms debounce pattern
  - onCitySelect callback with lat/lng/city
- TypeScript compiles: `cd apps/web && npx tsc --noEmit`
  </verify>
  <done>
CitySearchInput component exists with cities-only autocomplete, 350ms debounce, and coordinate extraction on selection.
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate CitySearchInput into LocationSearch</name>
  <files>apps/web/src/components/search/LocationSearch.tsx</files>
  <action>
Add city search as an expandable option in LocationSearch:

1. Add state: showCitySearch: boolean (default false)

2. Add "Cerca città" button next to existing location controls:
   - Small outline button with MapPin icon
   - Toggles showCitySearch state
   - Only show when location is NOT set, or as alternative to current location

3. Conditionally render CitySearchInput:
   - When showCitySearch is true, show CitySearchInput below the button row
   - Style as a collapsible section with smooth transition

4. Handle city selection:
   - onCitySelect callback: apply lat/lng to filters via updateFilter
   - Show toast "Cercando eventi a {city}"
   - Set showCitySearch to false after selection
   - Mark this location as city-based (add state to track: locationType: 'ip' | 'gps' | 'city')

5. Update location indicator:
   - When using city search, show city name instead of "(approssimativa)"
   - Example: "Milano" or "Roma" next to the location button
   - When using IP location, show "(rilevata automaticamente)"
   - When using GPS, show "(precisa)"

6. Clear functionality:
   - Clearing location should also reset locationType and hide city search
  </action>
  <verify>
- `cat apps/web/src/components/search/LocationSearch.tsx` shows:
  - CitySearchInput import and usage
  - showCitySearch state toggle
  - onCitySelect handler applying lat/lng
  - Location type indicator logic
- Start dev server and visit /discover
- Click "Cerca città" button
- Type "Roma" and see city suggestions
- Select Roma - filters update with Rome coordinates
- Location indicator shows "Roma"
  </verify>
  <done>
LocationSearch has city search option that allows users to explore events in any city for trip planning.
  </done>
</task>

</tasks>

<verification>
1. Visit /discover page
2. Click "Cerca città" button
3. Type "Milano" - see city suggestions with "Milano, Italy" etc
4. Select "Milano, Italy"
5. Observe: lat/lng applied to URL params, events refresh for Milan area
6. Location indicator shows "Milano"
7. Clear location - city search hides, indicator clears
8. Try searching for international city (e.g., "Barcelona") - works correctly
</verification>

<success_criteria>
- Users can search any city worldwide
- Only cities appear in suggestions (no venues, addresses)
- Selection applies coordinates and shows city name
- Works with existing Google Maps API key (no additional config)
- City search is discoverable but not intrusive
</success_criteria>

<output>
After completion, create `.planning/phases/06.3-location-ux-improvements/06.3-02-SUMMARY.md`
</output>
