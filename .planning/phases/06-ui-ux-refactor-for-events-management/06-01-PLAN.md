---
phase: 06-ui-ux-refactor-for-events-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/package.json
  - apps/web/src/locales/i18n.ts
  - apps/web/src/locales/it/common.json
  - apps/web/src/locales/it/events.json
  - apps/web/src/locales/it/forms.json
  - apps/web/src/locales/it/dashboard.json
  - apps/web/src/components/ui/date-picker.tsx
  - apps/web/src/main.tsx
autonomous: true

must_haves:
  truths:
    - "All UI text displays in Italian (buttons, labels, placeholders, validation messages)"
    - "Date inputs show Italian format (dd/MM/yyyy) with Italian month names"
    - "Date pickers display Italian day/month names (lun, gen, feb)"
  artifacts:
    - path: "apps/web/src/locales/i18n.ts"
      provides: "i18next configuration with Italian locale"
      min_lines: 30
    - path: "apps/web/src/locales/it/common.json"
      provides: "Shared Italian translations (buttons, validation)"
      min_lines: 20
    - path: "apps/web/src/components/ui/date-picker.tsx"
      provides: "Shadcn date picker with Italian locale"
      exports: ["DatePicker"]
  key_links:
    - from: "apps/web/src/main.tsx"
      to: "apps/web/src/locales/i18n.ts"
      via: "import i18n before React mount"
      pattern: "import.*i18n"
    - from: "apps/web/src/components/ui/date-picker.tsx"
      to: "date-fns/locale/it"
      via: "Italian locale prop to Calendar"
      pattern: "locale.*it"
---

<objective>
Establish Italian localization infrastructure and replace native date inputs with Italian-localized shadcn date pickers.

Purpose: Foundation for Phase 6 UI/UX improvements. All existing and new UI elements will display in Italian, and date inputs will show proper Italian formatting (dd/MM/yyyy, 24-hour time).

Output: react-i18next configured with Italian translations, shadcn date picker component with Italian locale ready for use throughout the app.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ui-ux-refactor-for-events-management/06-CONTEXT.md
@.planning/phases/06-ui-ux-refactor-for-events-management/06-RESEARCH.md
@.planning/codebase/STACK.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STRUCTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install react-i18next and configure Italian localization</name>
  <files>
    apps/web/package.json
    apps/web/src/locales/i18n.ts
    apps/web/src/locales/it/common.json
    apps/web/src/locales/it/events.json
    apps/web/src/locales/it/forms.json
    apps/web/src/locales/it/dashboard.json
    apps/web/src/main.tsx
  </files>
  <action>
    Install react-i18next and i18next:
    ```bash
    cd apps/web && pnpm install react-i18next i18next
    ```

    Create i18next configuration at apps/web/src/locales/i18n.ts:
    - Initialize i18next with initReactI18next
    - Set lng: 'it', fallbackLng: 'it'
    - Load namespace resources: common, events, forms, dashboard
    - Configure interpolation with date-fns formatting for Italian dates
    - Use date-fns Italian locale (it) for date formatting

    Create Italian translation files with namespaces:
    - apps/web/src/locales/it/common.json: Shared UI elements
      - buttons: save (Salva), cancel (Annulla), delete (Elimina), edit (Modifica), create (Crea), back (Indietro), next (Avanti), finish (Completa)
      - validation: required (Campo obbligatorio), invalid_email (Indirizzo email non valido), min_length, max_length
      - time: format_24h (HH:mm), format_date (dd/MM/yyyy), format_datetime (dd/MM/yyyy, HH:mm)

    - apps/web/src/locales/it/events.json: Event-specific translations
      - types: jam (Jam), class (Lezione), workshop (Workshop), convention (Convegno)
      - fields: title (Titolo), description (Descrizione), date (Data), time (Ora), location (Luogo), capacity (Capacità massima)
      - visibility_options: public (Pubblico - visibile a tutti), private (Privato - solo con link)
      - wizard: step titles, questions (Vuoi gestire i partecipanti?)

    - apps/web/src/locales/it/forms.json: Form-specific translations
      - labels, placeholders, help text

    - apps/web/src/locales/it/dashboard.json: Dashboard translations
      - sections: upcoming_events (Eventi imminenti), recommendations (Consigliati per te), statistics (Statistiche)

    Update apps/web/src/main.tsx:
    - Import i18n configuration before React mount: `import './locales/i18n'`
    - Ensure i18n initializes before rendering React app

    Reference RESEARCH.md Pattern 1 for implementation details.
  </action>
  <verify>
    - Check package.json includes react-i18next and i18next dependencies
    - Check i18n.ts exists and configures Italian locale with date-fns integration
    - Check all 4 translation JSON files exist with Italian content
    - Run `pnpm --filter @jamia/web build` to verify no import errors
    - Check main.tsx imports i18n before React mount
  </verify>
  <done>
    - react-i18next installed and configured with Italian as default language
    - Translation files created with comprehensive Italian UI text
    - i18n initialized before React app mount
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Italian-localized shadcn date picker component</name>
  <files>
    apps/web/src/components/ui/date-picker.tsx
  </files>
  <action>
    Create reusable DatePicker component at apps/web/src/components/ui/date-picker.tsx:
    - Import date-fns Italian locale: `import { it } from 'date-fns/locale/it'`
    - Use shadcn Popover + Calendar (react-day-picker) with Italian locale
    - Format display value with dd/MM/yyyy format using date-fns
    - Props: value (Date | undefined), onChange (Date | undefined) => void, placeholder (optional)
    - Apply locale={it} to Calendar component for Italian day/month names
    - Display trigger button with CalendarIcon and formatted date or placeholder text
    - Use Italian date format in button: format(value, 'dd/MM/yyyy', { locale: it })

    Component structure:
    - Popover wrapper with trigger Button (variant="outline")
    - Calendar with mode="single", selected, onSelect, locale=it, initialFocus
    - If no value: show placeholder "Seleziona data" (Italian for "Select date")
    - If value: show formatted Italian date (e.g., "22/01/2026")

    Reference RESEARCH.md Pattern 6 for implementation example.
    DO NOT use native HTML date inputs - only shadcn Calendar with Italian locale.
  </action>
  <verify>
    - Check date-picker.tsx exists in apps/web/src/components/ui/
    - Check component imports Italian locale from date-fns/locale/it
    - Check Calendar component receives locale={it} prop
    - Check date format uses dd/MM/yyyy with Italian locale
    - Run `pnpm --filter @jamia/web build` to verify component compiles
  </verify>
  <done>
    - DatePicker component created with Italian locale support
    - Component displays Italian month/day names (gen, lun, etc.)
    - Date format shows dd/MM/yyyy instead of MM/DD/YYYY
    - Ready for use in all forms throughout the app
  </done>
</task>

<task type="auto">
  <name>Task 3: Translate existing UI components to Italian</name>
  <files>
    apps/web/src/components/Footer.tsx
    apps/web/src/pages/Dashboard.tsx
    apps/web/src/pages/CreateJam.tsx
    apps/web/src/pages/CreateEvent.tsx
    apps/web/src/components/events/EventCard.tsx
    apps/web/src/components/search/LocationSearch.tsx
    apps/web/src/components/search/EventFilters.tsx
    apps/web/src/components/search/KeywordSearch.tsx
  </files>
  <action>
    Update key UI components to use Italian translations via useTranslation hook:

    1. Import useTranslation: `import { useTranslation } from 'react-i18next'`
    2. Use translation hook: `const { t } = useTranslation(['common', 'events'])`
    3. Replace hardcoded English text with t() calls

    Components to translate:
    - Footer.tsx: navigation links, copyright text
    - Dashboard.tsx: page title, section headings, button labels
    - CreateJam.tsx: form labels, placeholders, button text
    - CreateEvent.tsx: form labels, placeholders, button text
    - EventCard.tsx: event type badges, action buttons
    - LocationSearch.tsx: labels, placeholders, button text
    - EventFilters.tsx: filter labels, options
    - KeywordSearch.tsx: placeholder, label

    Example transformations:
    - "Create Event" → t('events:create_event')
    - "Save" → t('common:buttons.save')
    - "Cancel" → t('common:buttons.cancel')
    - "Event Type" → t('events:fields.type')
    - "Required field" → t('common:validation.required')

    For date formatting, use date-fns with Italian locale:
    - `format(date, 'dd/MM/yyyy', { locale: it })`
    - `format(time, 'HH:mm', { locale: it })`

    DO NOT translate user-generated content (event titles, descriptions) - only UI elements.
  </action>
  <verify>
    - Check each listed component imports useTranslation from react-i18next
    - Check hardcoded English strings replaced with t() calls
    - Check date formatting uses Italian locale from date-fns
    - Run `pnpm --filter @jamia/web build` to verify no translation key errors
    - Manually test in browser: UI displays Italian text
  </verify>
  <done>
    - All listed UI components display Italian text
    - Translation keys properly reference namespace files
    - Date/time displays use Italian formats (dd/MM/yyyy, HH:mm)
    - User-generated content remains untranslated
  </done>
</task>

</tasks>

<verification>
**Overall phase checks:**
- Run `pnpm --filter @jamia/web dev` and verify app starts without errors
- Open browser and confirm UI displays Italian text (buttons, labels, placeholders)
- Test date picker component: click date input, verify Italian month/day names (gen, lun)
- Verify date format displays as dd/MM/yyyy (not MM/DD/YYYY)
- Check console for missing translation key warnings

**Must-haves validation:**
- Truth 1: Navigate to Dashboard, CreateEvent, CreateJam - all buttons and labels in Italian
- Truth 2: Open any date picker - format shows dd/MM/yyyy with Italian month names
- Truth 3: Check Calendar component - day names show lun, mar, mer (not Mon, Tue, Wed)
</verification>

<success_criteria>
- react-i18next configured with Italian as default language
- Translation files created for common, events, forms, dashboard namespaces
- DatePicker component displays Italian month/day names and dd/MM/yyyy format
- Key UI components (Dashboard, CreateJam, CreateEvent, search filters) display Italian text
- No hardcoded English text in translated components
- Application builds and runs without translation errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-ui-ux-refactor-for-events-management/06-01-SUMMARY.md`
</output>
