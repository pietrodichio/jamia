---
phase: 06-ui-ux-refactor-for-events-management
plan: 07
type: execute
wave: 3
depends_on: [06-01, 06-02]
files_modified:
  - apps/web/src/pages/EventDetail.tsx
  - apps/web/src/components/events/EventHero.tsx
  - apps/web/src/components/events/EventOrganizerInfo.tsx
  - apps/web/src/components/events/TeachersList.tsx
  - apps/web/src/components/events/EventActions.tsx
  - apps/web/src/lib/calendar-export.ts
autonomous: true

must_haves:
  truths:
    - "Event detail page shows hero image at top, followed by event info"
    - "Organizer details hidden if organizer is super admin"
    - "Teachers displayed with photo and full name for classes/workshops/conventions"
    - "External registration shows custom CTA button linking to external URL"
    - "Users can add event to calendar (Google Calendar, iCal export)"
  artifacts:
    - path: "apps/web/src/pages/EventDetail.tsx"
      provides: "Enhanced event detail layout"
      min_lines: 100
    - path: "apps/web/src/components/events/EventHero.tsx"
      provides: "Hero image section with event type badge"
      min_lines: 40
    - path: "apps/web/src/components/events/EventActions.tsx"
      provides: "Action buttons (register, add to calendar, share, edit)"
      min_lines: 60
    - path: "apps/web/src/lib/calendar-export.ts"
      provides: "Utility to generate iCal data"
      min_lines: 50
  key_links:
    - from: "apps/web/src/components/events/EventActions.tsx"
      to: "apps/web/src/lib/calendar-export.ts"
      via: "generates iCal data for download"
      pattern: "generateICalData"
    - from: "apps/web/src/pages/EventDetail.tsx"
      to: "apps/web/src/components/events/EventOrganizerInfo.tsx"
      via: "conditional display based on super admin status"
      pattern: "!isSuperAdmin.*EventOrganizerInfo"
---

<objective>
Enhance event detail page with hero layout, organizer visibility logic, teacher display, and calendar export.

Purpose: Provide comprehensive event information with clear visual hierarchy and useful actions (register, export, share).

Output: Redesigned EventDetail page with hero section, organizer/teacher info, external registration support, and calendar export functionality.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ui-ux-refactor-for-events-management/06-CONTEXT.md
@.planning/phases/06-ui-ux-refactor-for-events-management/06-RESEARCH.md
@.planning/phases/06-ui-ux-refactor-for-events-management/06-01-SUMMARY.md
@.planning/phases/06-ui-ux-refactor-for-events-management/06-02-SUMMARY.md
@.planning/codebase/STACK.md
@.planning/codebase/CONVENTIONS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create EventHero and EventOrganizerInfo components</name>
  <files>
    apps/web/src/components/events/EventHero.tsx
    apps/web/src/components/events/EventOrganizerInfo.tsx
  </files>
  <action>
    Create EventHero at apps/web/src/components/events/EventHero.tsx:
    - Props: event (EventWithOrganizer from @jamia/types)
    - Import Badge from shadcn/ui
    - Import useTranslation from react-i18next
    - Import getOptimizedImageUrl from @/lib/image-utils

    Hero structure:
    1. Image container (relative, h-96 w-full):
       - Full-width hero image with optimization
       - Use getOptimizedImageUrl with width: 1200, quality: 90
       - Object-fit: cover
       - If no image: use placeholder gradient or default image

    2. Overlay content (absolute bottom-0 left-0 right-0):
       - Dark gradient overlay for text readability
       - Event type Badge (top-right, absolute top-4 right-4)
       - Event title (h1, text-4xl font-bold, text-white)
       - Event date/time (text-xl, text-white/90)
         - Format: format(date, 'dd MMMM yyyy, HH:mm', { locale: it })

    3. Styling:
       - Gradient overlay: bg-gradient-to-t from-black/80 to-transparent
       - Padding: p-8 for overlay content
       - Responsive: smaller text on mobile

    Create EventOrganizerInfo at apps/web/src/components/events/EventOrganizerInfo.tsx:
    - Props: organizer (Profile type), isSuperAdmin (boolean)
    - Import Card, CardHeader, CardTitle, CardContent from shadcn/ui
    - Import UserAvatar from existing component
    - Import useTranslation

    Conditional rendering:
    - If isSuperAdmin: return null (hide organizer details per CONTEXT.md)
    - Otherwise: display organizer info

    Organizer display:
    1. Card with header "Organizzatore" (Organizer)
    2. Content:
       - Avatar (UserAvatar component)
       - Full name
       - Bio (if provided)
       - Contact button (if contact info available): "Contatta" (Contact)

    Reference CONTEXT.md Event Detail Page section: "If organizer is super admin: hide organizer details".
  </action>
  <verify>
    - Check EventHero.tsx exists with full-width image and overlay
    - Check hero uses optimized image URL (width: 1200)
    - Check date formatting uses Italian locale
    - Check EventOrganizerInfo.tsx exists with conditional rendering
    - Check component returns null if isSuperAdmin is true
    - Run `pnpm --filter @jamia/web build` to verify components compile
  </verify>
  <done>
    - EventHero displays full-width hero image with event info overlay
    - Event type badge, title, and Italian-formatted date shown on hero
    - EventOrganizerInfo hides details if organizer is super admin
    - Otherwise displays organizer avatar, name, bio, contact option
  </done>
</task>

<task type="auto">
  <name>Task 2: Create TeachersList and calendar export utility</name>
  <files>
    apps/web/src/components/events/TeachersList.tsx
    apps/web/src/lib/calendar-export.ts
  </files>
  <action>
    Create TeachersList at apps/web/src/components/events/TeachersList.tsx:
    - Props: teachers (Profile[]), eventType (EventType)
    - Import Card, CardHeader, CardTitle, CardContent from shadcn/ui
    - Import UserAvatar from existing component
    - Import useTranslation

    Conditional rendering:
    - Only display if eventType is 'class', 'workshop', or 'convention'
    - If eventType is 'jam': return null
    - If teachers array empty: return null

    Teachers display:
    1. Card with header "Insegnanti" (Teachers) or "Facilitatori" (Facilitators)
    2. Grid layout for multiple teachers:
       - Desktop: 2-3 columns
       - Mobile: 1 column
    3. Each teacher card:
       - Avatar (large, circular)
       - Full name below avatar
       - Clickable: navigate to /teachers/{teacherId} (existing route)

    Styling:
    - Clean grid with gap-4
    - Each teacher: centered layout, text-center
    - Hover effect: subtle scale or shadow

    Reference CONTEXT.md Event Detail Page: "Teachers: Display photo and full name. Social profiles and teacher invitations: deferred to future phase".

    Create calendar export utility at apps/web/src/lib/calendar-export.ts:
    - Export function generateICalData(event: Event): string
    - Generate iCalendar (.ics) format string

    iCal structure:
    ```
    BEGIN:VCALENDAR
    VERSION:2.0
    PRODID:-//Jamia//Event//EN
    BEGIN:VEVENT
    UID:{event.id}@jamia.app
    DTSTAMP:{current timestamp in iCal format}
    DTSTART:{event.starts_at in iCal format}
    DTEND:{event.ends_at in iCal format}
    SUMMARY:{event.title}
    DESCRIPTION:{event.description}
    LOCATION:{event.location}
    URL:{window.location.origin}/events/{event.id}
    END:VEVENT
    END:VCALENDAR
    ```

    Date format helper:
    - Convert ISO date to iCal format: YYYYMMDDTHHMMSSZ
    - Use date-fns format: format(date, "yyyyMMdd'T'HHmmss'Z'")

    Export function downloadICalFile(event: Event):
    - Generate iCal data
    - Create Blob with type 'text/calendar'
    - Trigger download with filename: `${event.title.replace(/\s+/g, '-')}.ics`

    Reference: iCalendar specification (RFC 5545) for format details.
  </action>
  <verify>
    - Check TeachersList.tsx exists with conditional rendering
    - Check component only displays for classes, workshops, conventions
    - Check teachers displayed in grid with avatar and name
    - Check calendar-export.ts exists with iCal generation
    - Check iCal format includes required fields (UID, DTSTART, DTEND, SUMMARY)
    - Run `pnpm --filter @jamia/web build` to verify utility compiles
  </verify>
  <done>
    - TeachersList displays teachers for classes/workshops/conventions
    - Teacher cards show avatar, name, link to profile
    - calendar-export utility generates valid iCal data
    - downloadICalFile triggers .ics download with event data
  </done>
</task>

<task type="auto">
  <name>Task 3: Enhance EventDetail page with new components and actions</name>
  <files>
    apps/web/src/pages/EventDetail.tsx
    apps/web/src/components/events/EventActions.tsx
  </files>
  <action>
    Create EventActions at apps/web/src/components/events/EventActions.tsx:
    - Props: event (EventWithOrganizer), isOwner (boolean)
    - Import Button from shadcn/ui
    - Import useTranslation from react-i18next
    - Import useNavigate from react-router-dom
    - Import downloadICalFile from @/lib/calendar-export
    - Import icons from lucide-react (Calendar, Share, Edit)

    Action buttons:
    1. Register/RSVP (if managed jam with participant management):
       - Button: "Iscriviti" (Register)
       - Action: navigate to jam RSVP flow
       - Only show if event.type === 'jam' AND event.manage_participants

    2. External registration (if external_link exists):
       - Button with custom CTA text: event.cta_text || "Registrati"
       - Action: open external link in new tab
       - Show icon based on URL (Instagram, Google Forms, email)
       - Only show if event.external_link exists

    3. Add to calendar:
       - Button: "Aggiungi al calendario" (Add to calendar)
       - Dropdown or direct download:
         - Google Calendar (opens URL with query params)
         - iCal download (uses downloadICalFile)

    4. Share event:
       - Button: "Condividi" (Share)
       - Options:
         - Copy link to clipboard
         - Share via Web Share API (if supported)
       - Toast notification on success

    5. Edit event (if isOwner):
       - Button: "Modifica" (Edit)
       - Action: navigate to /events/{eventId}/edit
       - variant="outline"

    Layout: Horizontal button group with gap-2, wrap on mobile

    Google Calendar link helper:
    ```typescript
    function generateGoogleCalendarUrl(event: Event): string {
      const startDate = format(new Date(event.starts_at), "yyyyMMdd'T'HHmmss");
      const endDate = format(new Date(event.ends_at || event.starts_at), "yyyyMMdd'T'HHmmss");
      const params = new URLSearchParams({
        action: 'TEMPLATE',
        text: event.title,
        dates: `${startDate}/${endDate}`,
        details: event.description || '',
        location: event.location || '',
      });
      return `https://calendar.google.com/calendar/render?${params}`;
    }
    ```

    Redesign EventDetail.tsx:
    - Import EventHero, EventOrganizerInfo, TeachersList, EventActions
    - Import useQuery to fetch event data
    - Import useParams to get eventId from route
    - Import useTranslation

    Page layout:
    1. EventHero at top (full width, hero image)

    2. Content container (max-w-5xl mx-auto px-4 py-8):
       - Grid layout: main content (left 2/3), sidebar (right 1/3)

    3. Main content column:
       a. EventActions (sticky or at top)
       b. Description section:
          - Heading: "Descrizione"
          - Event description text
       c. Schedule section:
          - Start/end dates with Italian formatting
          - Recurring event info (if applicable, show RRULE human-readable)
       d. TeachersList (if teachers exist)
       e. Additional details:
          - Price (if set, show "€X" or "Gratuito")
          - Tags (if exist)
          - Amenities (accommodation, food)

    4. Sidebar column:
       - EventOrganizerInfo (conditional on super admin status)
       - Location map (future enhancement - placeholder or static map)
       - Share section

    5. Responsive:
       - Desktop: 2-column layout (content + sidebar)
       - Mobile: single column stack (content, then sidebar items)

    Reference CONTEXT.md Event Detail Page section for complete requirements.
  </action>
  <verify>
    - Check EventActions.tsx exists with all action buttons
    - Check external registration button displays custom CTA text
    - Check "Add to calendar" provides Google Calendar and iCal options
    - Check EventDetail.tsx integrates all new components
    - Check layout: hero at top, then content grid
    - Check organizer info hidden if super admin
    - Check teachers list displays for classes/workshops/conventions
    - Run `pnpm --filter @jamia/web dev` and navigate to /events/{id}
    - Test actions: add to calendar downloads .ics file, share copies link
    - Verify Italian labels on all buttons and sections
  </verify>
  <done>
    - EventDetail page displays hero image with event info overlay
    - EventActions provides register, add to calendar, share, edit buttons
    - External registration button shows custom CTA text and opens external link
    - Add to calendar supports Google Calendar and iCal download
    - Organizer info hidden if organizer is super admin
    - Teachers list displays for classes, workshops, conventions
    - All Italian translations applied to detail page
  </done>
</task>

</tasks>

<verification>
**Overall phase checks:**
- Run `pnpm --filter @jamia/web dev`
- Navigate to /events/{eventId} for various event types
- Test managed jam:
  - Verify hero image displays with event info overlay
  - Verify "Iscriviti" button appears
  - Verify organizer info shows (if not super admin)
- Test class with teachers:
  - Verify teachers list displays with avatars and names
  - Verify no "manage participants" options
- Test event with external link:
  - Verify custom CTA button displays (e.g., "Contattaci")
  - Click button: opens external URL in new tab
- Test calendar export:
  - Click "Aggiungi al calendario"
  - Select iCal: downloads .ics file
  - Select Google Calendar: opens Google Calendar in new tab
- Test share:
  - Click "Condividi"
  - Verify link copied to clipboard, toast notification appears
- Test edit button (if owner):
  - Verify "Modifica" button appears
  - Click: navigates to edit page

**Must-haves validation:**
- Truth 1: Hero image at top with event title, date overlay
- Truth 2: Super admin event: organizer details hidden
- Truth 3: Class event: teachers displayed with photo and name
- Truth 4: External link event: custom CTA button opens external URL
- Truth 5: "Add to calendar" downloads .ics file or opens Google Calendar
</verification>

<success_criteria>
- EventHero displays full-width hero image with event info overlay
- EventOrganizerInfo conditionally hides if organizer is super admin
- TeachersList displays teachers for classes, workshops, conventions
- EventActions provides register, external link, add to calendar, share, edit
- Calendar export generates valid iCal data and downloads .ics file
- Google Calendar integration opens pre-filled event creation
- Share function copies link to clipboard with toast notification
- All Italian labels and translations applied to detail page
</success_criteria>

<output>
After completion, create `.planning/phases/06-ui-ux-refactor-for-events-management/06-07-SUMMARY.md`
</output>
