---
phase: 06-ui-ux-refactor-for-events-management
plan: 03
type: execute
wave: 2
depends_on: [06-01, 06-02]
files_modified:
  - apps/web/src/pages/Index.tsx
  - apps/web/src/pages/DiscoverEvents.tsx
  - apps/web/src/pages/EventCalendar.tsx
  - apps/web/src/App.tsx
  - apps/web/src/components/events/UnifiedEventView.tsx
  - apps/web/src/components/marketing/HeroSection.tsx
autonomous: true

must_haves:
  truths:
    - "Home page (/) displays events with marketing content for logged-out users"
    - "Unified /discover route shows events in grid or calendar view with toggle"
    - "/calendar route redirects to /discover?view=calendar"
    - "Filter bar sticky at top, shared across grid and calendar views"
    - "URL parameters persist view state (bookmarkable)"
  artifacts:
    - path: "apps/web/src/pages/Index.tsx"
      provides: "Airbnb-style home page with events + marketing"
      min_lines: 50
    - path: "apps/web/src/components/events/UnifiedEventView.tsx"
      provides: "Single view component with grid/calendar toggle"
      min_lines: 80
  key_links:
    - from: "apps/web/src/components/events/UnifiedEventView.tsx"
      to: "URLSearchParams"
      via: "view parameter in URL"
      pattern: "searchParams.get\\('view'\\)"
    - from: "apps/web/src/pages/Index.tsx"
      to: "apps/web/src/components/events/EventCardGrid.tsx"
      via: "displays events on home page"
      pattern: "EventCardGrid"
---

<objective>
Redesign home page with Airbnb-style events display and merge calendar/discover views into single unified experience.

Purpose: Improve discovery UX by showing events immediately on home page and providing seamless grid/calendar toggle on discovery route.

Output: Redesigned home page with marketing content, unified /discover route with view toggle, /calendar redirect to /discover?view=calendar.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ui-ux-refactor-for-events-management/06-CONTEXT.md
@.planning/phases/06-ui-ux-refactor-for-events-management/06-RESEARCH.md
@.planning/phases/06-ui-ux-refactor-for-events-management/06-01-SUMMARY.md
@.planning/phases/06-ui-ux-refactor-for-events-management/06-02-SUMMARY.md
@.planning/codebase/STACK.md
@.planning/codebase/CONVENTIONS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create marketing HeroSection component</name>
  <files>
    apps/web/src/components/marketing/HeroSection.tsx
  </files>
  <action>
    Create HeroSection component at apps/web/src/components/marketing/HeroSection.tsx:
    - Purpose: Marketing content for home page (logged-out users)
    - Import useTranslation from react-i18next
    - Import Button from @/components/ui/button
    - Import useNavigate from react-router-dom

    Hero structure:
    1. Heading (h1, text-4xl font-bold):
       - Italian: "Scopri eventi di acroyoga vicino a te"
       - Translation key: t('marketing:hero.title')

    2. Subheading (text-xl text-muted-foreground):
       - Italian: "Jam, lezioni, workshop e convegni nella tua zona"
       - Translation key: t('marketing:hero.subtitle')

    3. CTA buttons:
       - Primary: "Esplora Eventi" (navigates to /discover)
       - Secondary: "Crea Evento" (navigates to /create-event)

    4. Styling:
       - Container: max-w-4xl mx-auto text-center py-12
       - Spacing: mb-6 for heading, mb-8 for subtitle, gap-4 for buttons

    Add translation keys to apps/web/src/locales/it/marketing.json:
    - hero.title, hero.subtitle, hero.cta_explore, hero.cta_create

    Reference CONTEXT.md: "Home page shows events + marketing content for logged-out users (Airbnb-style approach)".
  </action>
  <verify>
    - Check HeroSection.tsx exists in apps/web/src/components/marketing/
    - Check component uses Italian translation keys
    - Check CTA buttons navigate to /discover and /create-event
    - Check translation file marketing.json created with hero keys
    - Run `pnpm --filter @jamia/web build` to verify component compiles
  </verify>
  <done>
    - HeroSection displays Italian marketing content
    - CTA buttons navigate to discovery and creation flows
    - Component ready for home page integration
  </done>
</task>

<task type="auto">
  <name>Task 2: Redesign home page with events and marketing</name>
  <files>
    apps/web/src/pages/Index.tsx
  </files>
  <action>
    Redesign Index.tsx (home page at / route):
    - Import HeroSection from @/components/marketing/HeroSection
    - Import EventCardGrid from @/components/events/EventCardGrid
    - Import useEventFilters hook from existing search hooks
    - Import useQuery from @tanstack/react-query
    - Import searchEvents from @/api/events.api

    Page structure:
    1. HeroSection at top (always visible)

    2. Events section below:
       - Heading: "Eventi in programma" (Upcoming Events)
       - Fetch recent/nearby events (limit 6-9 for preview)
       - Display in EventCardGrid
       - "Vedi tutti" (View all) link to /discover

    3. Conditional rendering:
       - If user logged in AND has location: show nearby events
       - If logged out OR no location: show recent events (sorted by date)

    Query for events:
    - Use searchEvents API with default filters (no location required)
    - If user has saved location in profile: use it
    - Otherwise: fetch events without location filter, sort by date
    - Limit: 9 events

    Styling:
    - Container: max-w-7xl mx-auto px-4 py-8
    - Spacing between sections: space-y-12

    Reference CONTEXT.md: "Home page (/) shows events + marketing content for logged-out users (Airbnb-style approach)".
  </action>
  <verify>
    - Check Index.tsx renders HeroSection at top
    - Check EventCardGrid displays below with events
    - Check "Vedi tutti" link navigates to /discover
    - Check events fetch with searchEvents API
    - Run `pnpm --filter @jamia/web dev` and navigate to / route
    - Verify hero content displays in Italian
  </verify>
  <done>
    - Home page displays marketing hero section
    - Events grid shows preview of upcoming events (6-9 cards)
    - "View all" link navigates to /discover
    - Page works for both logged-in and logged-out users
  </done>
</task>

<task type="auto">
  <name>Task 3: Create unified discovery view and merge calendar route</name>
  <files>
    apps/web/src/components/events/UnifiedEventView.tsx
    apps/web/src/pages/DiscoverEvents.tsx
    apps/web/src/pages/EventCalendar.tsx
    apps/web/src/App.tsx
  </files>
  <action>
    Create UnifiedEventView component at apps/web/src/components/events/UnifiedEventView.tsx:
    - Import useSearchParams from react-router-dom
    - Import ViewToggle from @/components/events/ViewToggle
    - Import EventCardGrid from @/components/events/EventCardGrid
    - Import CalendarView from existing component (apps/web/src/components/events/CalendarView.tsx)
    - Import LocationSearch, EventFilters, KeywordSearch from search components
    - Import useEventFilters hook
    - Import useQuery from @tanstack/react-query

    Component logic:
    1. Read view from URL: `const view = searchParams.get('view') || 'grid'`
    2. Set view: `setSearchParams(params => { params.set('view', newView); return params; }, { replace: true })`
    3. Read filter params from URL using useEventFilters hook
    4. Fetch events with searchEvents API using current filters

    Layout structure:
    1. Filter bar (sticky top-0 z-10 bg-background):
       - LocationSearch
       - EventFilters
       - KeywordSearch
       - All in horizontal layout with gap

    2. ViewToggle below filters:
       - Position: flex justify-end mb-4
       - Passes currentView and onViewChange handler

    3. Results area:
       - If view === 'grid': render EventCardGrid
       - If view === 'calendar': render CalendarView
       - Both receive same events data and filters

    4. Loading and empty states:
       - Show skeleton loader while fetching
       - Show Italian empty state: "Nessun evento trovato"

    Update DiscoverEvents.tsx:
    - Replace existing content with UnifiedEventView component
    - Keep existing route at /discover

    Update EventCalendar.tsx:
    - Replace with redirect to /discover?view=calendar
    - Use Navigate component: `<Navigate to="/discover?view=calendar" replace />`

    Update App.tsx routes:
    - Keep /discover route pointing to DiscoverEvents page
    - Keep /calendar route pointing to EventCalendar page (which now redirects)

    Reference RESEARCH.md Pattern 4 for URL state management and view toggle implementation.
  </action>
  <verify>
    - Check UnifiedEventView.tsx exists with view toggle logic
    - Check URL parameter 'view' controls grid vs calendar display
    - Check DiscoverEvents.tsx uses UnifiedEventView component
    - Check EventCalendar.tsx redirects to /discover?view=calendar
    - Run `pnpm --filter @jamia/web dev` and test:
      - Navigate to /discover: should show grid view by default
      - Click calendar toggle: URL changes to /discover?view=calendar
      - Navigate to /calendar: should redirect to /discover?view=calendar
      - Refresh page: view state persists (from URL)
  </verify>
  <done>
    - UnifiedEventView merges grid and calendar into single component
    - View state managed via URL parameter (bookmarkable)
    - /discover route shows grid view by default
    - /calendar route redirects to /discover?view=calendar
    - Filter bar shared across both views (sticky at top)
    - View toggle switches between grid and calendar seamlessly
  </done>
</task>

</tasks>

<verification>
**Overall phase checks:**
- Run `pnpm --filter @jamia/web dev`
- Navigate to / (home): verify hero section + event cards display
- Navigate to /discover: verify grid view shows, filter bar sticky at top
- Click calendar toggle: verify view switches to calendar, URL updates
- Navigate to /calendar: verify redirect to /discover?view=calendar
- Apply filters: verify same filters work in both grid and calendar views
- Check URL: verify view parameter persists on refresh

**Must-haves validation:**
- Truth 1: Home page shows HeroSection + EventCardGrid with events
- Truth 2: /discover route displays UnifiedEventView with toggle
- Truth 3: /calendar redirects to /discover?view=calendar
- Truth 4: Filter bar sticky at top, visible in both views
- Truth 5: Bookmark /discover?view=calendar, reopen - view state persists
</verification>

<success_criteria>
- Home page displays marketing hero + event preview grid
- UnifiedEventView component merges grid and calendar views
- ViewToggle switches between views with URL state management
- /calendar route redirects to /discover?view=calendar
- Filter bar shared across grid and calendar views
- URL parameters make views bookmarkable and shareable
- All Italian translations applied to new components
</success_criteria>

<output>
After completion, create `.planning/phases/06-ui-ux-refactor-for-events-management/06-03-SUMMARY.md`
</output>
