---
phase: 06-ui-ux-refactor-for-events-management
plan: 06
type: execute
wave: 3
depends_on: [06-01, 06-02]
files_modified:
  - apps/web/src/pages/Dashboard.tsx
  - apps/web/src/components/dashboard/UpcomingEventsSection.tsx
  - apps/web/src/components/dashboard/RecommendationsSection.tsx
  - apps/web/src/components/dashboard/StatisticsSection.tsx
  - apps/web/src/components/dashboard/DashboardActions.tsx
autonomous: true

must_haves:
  truths:
    - "Dashboard shows information first (what's happening), actions second"
    - "Upcoming events section displays next 3-5 events with 'View all' link"
    - "Recommendations section shows nearby events based on user location"
    - "Statistics section displays event counts and activity metrics"
  artifacts:
    - path: "apps/web/src/pages/Dashboard.tsx"
      provides: "Redesigned information-oriented dashboard"
      min_lines: 80
    - path: "apps/web/src/components/dashboard/UpcomingEventsSection.tsx"
      provides: "Preview of user's upcoming events"
      min_lines: 50
    - path: "apps/web/src/components/dashboard/RecommendationsSection.tsx"
      provides: "Personalized event suggestions"
      min_lines: 60
    - path: "apps/web/src/components/dashboard/StatisticsSection.tsx"
      provides: "User activity and event statistics"
      min_lines: 40
  key_links:
    - from: "apps/web/src/components/dashboard/RecommendationsSection.tsx"
      to: "apps/web/src/api/events.api.ts"
      via: "fetches nearby events with user location"
      pattern: "searchEvents.*lat.*lng"
    - from: "apps/web/src/components/dashboard/UpcomingEventsSection.tsx"
      to: "apps/web/src/components/events/EventCard.tsx"
      via: "displays events as cards"
      pattern: "EventCard"
---

<objective>
Redesign dashboard with information-first focus showing upcoming events, recommendations, and statistics.

Purpose: Transform dashboard from action-oriented to information-oriented, helping users see what's happening with their events and discover nearby activities.

Output: Redesigned Dashboard page with four sections: upcoming events, recommendations, statistics, and compact actions.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ui-ux-refactor-for-events-management/06-CONTEXT.md
@.planning/phases/06-ui-ux-refactor-for-events-management/06-RESEARCH.md
@.planning/phases/06-ui-ux-refactor-for-events-management/06-01-SUMMARY.md
@.planning/phases/06-ui-ux-refactor-for-events-management/06-02-SUMMARY.md
@.planning/codebase/STACK.md
@.planning/codebase/CONVENTIONS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create UpcomingEventsSection component</name>
  <files>
    apps/web/src/components/dashboard/UpcomingEventsSection.tsx
  </files>
  <action>
    Create UpcomingEventsSection at apps/web/src/components/dashboard/UpcomingEventsSection.tsx:
    - Import useQuery from @tanstack/react-query
    - Import useTranslation from react-i18next
    - Import EventCard from @/components/events/EventCard
    - Import Card, CardHeader, CardTitle, CardContent from shadcn/ui
    - Import Button from shadcn/ui (for "View all" link)
    - Import useNavigate from react-router-dom

    Query logic:
    - Fetch user's owned events: GET /api/events?owner_id={userId}
    - Fetch co-organized events: GET /api/events?co_organizer_id={userId}
    - Merge and sort by date (earliest first)
    - Filter: only future events (starts_at > now)
    - Limit: 5 events

    Component structure:
    1. Card wrapper with header:
       - Title: t('dashboard:sections.upcoming_events') - "I tuoi eventi imminenti"
       - "Vedi tutti" link in header (navigates to /events?filter=mine)

    2. Event list:
       - If events exist: render EventCard for each (horizontal layout or small cards)
       - If no events: show empty state message
         - Italian: "Non hai eventi in programma"
         - CTA button: "Crea un evento" (navigates to /create-event)

    3. Styling:
       - Cards in horizontal scroll or grid (max 3 visible, scroll for more)
       - Compact card variant (smaller than discovery cards)

    Reference CONTEXT.md Dashboard Integration section.
  </action>
  <verify>
    - Check UpcomingEventsSection.tsx exists in apps/web/src/components/dashboard/
    - Check component fetches user's owned and co-organized events
    - Check events sorted by date, filtered for future only
    - Check "Vedi tutti" link navigates to filtered view
    - Check empty state displays Italian message with CTA
    - Run `pnpm --filter @jamia/web build` to verify component compiles
  </verify>
  <done>
    - UpcomingEventsSection displays next 5 events
    - "View all" link navigates to full event list
    - Empty state encourages event creation
    - Component ready for dashboard integration
  </done>
</task>

<task type="auto">
  <name>Task 2: Create RecommendationsSection and StatisticsSection</name>
  <files>
    apps/web/src/components/dashboard/RecommendationsSection.tsx
    apps/web/src/components/dashboard/StatisticsSection.tsx
  </files>
  <action>
    Create RecommendationsSection at apps/web/src/components/dashboard/RecommendationsSection.tsx:
    - Import useQuery from @tanstack/react-query
    - Import searchEvents from @/api/events.api
    - Import EventCard from @/components/events/EventCard
    - Import Card, CardHeader, CardTitle, CardContent from shadcn/ui
    - Import useTranslation

    Query logic:
    1. Fetch user profile to get saved location (lat/lng)
    2. If user has location:
       - Call searchEvents({ lat, lng, radius: 50000, limit: 5 })
       - Filter out user's own events (exclude events where owner_id = userId)
    3. If no location:
       - Show prompt: "Imposta la tua posizione per vedere eventi vicini" (Set location to see nearby events)
       - CTA button: "Vai al Profilo" (navigates to /profile)

    Component structure:
    - Card wrapper with header:
      - Title: t('dashboard:sections.recommendations') - "Consigliati per te"
    - Event cards in horizontal scroll or grid (max 5)
    - If no recommendations: show message encouraging location setup

    Create StatisticsSection at apps/web/src/components/dashboard/StatisticsSection.tsx:
    - Import useQuery from @tanstack/react-query
    - Import Card, CardContent from shadcn/ui
    - Import useTranslation

    Statistics to display:
    1. Total events created (owned + co-organized)
    2. Total participants managed (across all jams, if managing)
    3. Events attended (future enhancement - placeholder for now)

    Query logic:
    - Count owned events: GET /api/events?owner_id={userId}&count=true
    - Count co-organized events: GET /api/events?co_organizer_id={userId}&count=true
    - Count participants: GET /api/participants?jam_owner_id={userId}&count=true (if managing jams)

    Component structure:
    - Grid layout with 3 stat cards (or single card with 3 sections)
    - Each stat shows:
      - Number (large, bold)
      - Label (Italian: "Eventi creati", "Partecipanti gestiti", "Eventi partecipati")
    - Clean, minimal design with clear hierarchy

    Reference CONTEXT.md Dashboard Integration section and RESEARCH.md "Dashboard Recommendations" pattern.
  </action>
  <verify>
    - Check RecommendationsSection.tsx exists and fetches nearby events
    - Check component uses user location from profile
    - Check empty state prompts for location setup
    - Check StatisticsSection.tsx exists and displays 3 metrics
    - Check statistics query user's events and participants
    - Run `pnpm --filter @jamia/web build` to verify components compile
  </verify>
  <done>
    - RecommendationsSection shows nearby events based on user location
    - Location prompt displayed if user hasn't set location
    - StatisticsSection displays event and participant counts
    - Both components ready for dashboard integration
  </done>
</task>

<task type="auto">
  <name>Task 3: Redesign Dashboard page with information-first layout</name>
  <files>
    apps/web/src/pages/Dashboard.tsx
    apps/web/src/components/dashboard/DashboardActions.tsx
  </files>
  <action>
    Create DashboardActions at apps/web/src/components/dashboard/DashboardActions.tsx:
    - Import Button from shadcn/ui
    - Import useNavigate from react-router-dom
    - Import useTranslation
    - Import icons from lucide-react (Plus, Search, Calendar)

    Action buttons (compact, in header or sidebar):
    1. "Crea Evento" (navigates to /create-event)
    2. "Scopri Eventi" (navigates to /discover)
    3. "Calendario" (navigates to /discover?view=calendar)

    Layout: Horizontal button group or vertical stack, compact sizing

    Redesign Dashboard.tsx:
    - Import UpcomingEventsSection, RecommendationsSection, StatisticsSection
    - Import DashboardActions
    - Import useTranslation

    New layout structure:
    1. Page header:
       - Title: t('dashboard:title') - "Bacheca" or "Dashboard"
       - DashboardActions in top-right corner (compact)

    2. Main content (grid layout):
       - Top row (full width): StatisticsSection
       - Middle row: UpcomingEventsSection
       - Bottom row: RecommendationsSection

    3. Responsive layout:
       - Desktop: 2-column grid (statistics top full width, upcoming left, recommendations right)
       - Mobile: single column stack (statistics, upcoming, recommendations)

    4. Spacing:
       - Container: max-w-7xl mx-auto px-4 py-8
       - Grid gap: gap-6 or gap-8
       - Section spacing: space-y-6

    Remove or minimize:
    - Large action buttons in content area
    - Redundant navigation elements
    - Overly prominent CTAs

    Focus on information density:
    - Show data first (events, stats)
    - Actions accessible but not dominant
    - Clean visual hierarchy

    Reference CONTEXT.md Dashboard Integration section: "Information-oriented focus: show what's happening, not just quick actions".
  </action>
  <verify>
    - Check DashboardActions.tsx exists with compact action buttons
    - Check Dashboard.tsx imports all section components
    - Check layout prioritizes information (statistics, events, recommendations)
    - Check action buttons positioned in header (not content area)
    - Check responsive layout works on mobile (single column stack)
    - Run `pnpm --filter @jamia/web dev` and navigate to /dashboard
    - Verify sections display in correct order: stats, upcoming, recommendations
    - Test action buttons: navigate to create-event, discover, calendar
  </verify>
  <done>
    - Dashboard displays information-first layout
    - Statistics section shows event and participant counts
    - Upcoming events section displays next 5 events
    - Recommendations section shows nearby events
    - Action buttons compact and positioned in header
    - Responsive layout adapts to mobile (single column)
    - All Italian labels and translations applied
  </done>
</task>

</tasks>

<verification>
**Overall phase checks:**
- Run `pnpm --filter @jamia/web dev`
- Navigate to /dashboard
- Verify layout: Statistics at top, Upcoming Events and Recommendations below
- Check UpcomingEventsSection: displays user's events, "Vedi tutti" link works
- Check RecommendationsSection: shows nearby events or location prompt
- Check StatisticsSection: displays event counts
- Test action buttons in header: "Crea Evento", "Scopri Eventi", "Calendario"
- Resize browser: verify responsive layout (mobile single column)

**Must-haves validation:**
- Truth 1: Dashboard shows sections with information (events, stats) before actions
- Truth 2: Upcoming events shows 3-5 events with "View all" link to filtered list
- Truth 3: Recommendations shows nearby events based on user's saved location
- Truth 4: Statistics displays total events created and participants managed
</verification>

<success_criteria>
- Dashboard redesigned with information-first layout
- UpcomingEventsSection displays user's next 5 events with "View all" link
- RecommendationsSection shows nearby events or prompts for location
- StatisticsSection displays event and participant counts
- DashboardActions provides compact action buttons in header
- Responsive layout adapts to mobile devices
- All Italian translations applied to sections and actions
</success_criteria>

<output>
After completion, create `.planning/phases/06-ui-ux-refactor-for-events-management/06-06-SUMMARY.md`
</output>
