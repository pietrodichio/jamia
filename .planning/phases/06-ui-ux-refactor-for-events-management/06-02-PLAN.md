---
phase: 06-ui-ux-refactor-for-events-management
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/components/events/EventCardGrid.tsx
  - apps/web/src/components/events/EventCard.tsx
  - apps/web/src/components/events/ViewToggle.tsx
  - apps/web/src/lib/image-utils.ts
autonomous: true

must_haves:
  truths:
    - "Event cards display in responsive grid (3 cols desktop, 2 cols tablet, 1 col mobile)"
    - "Cards show hero image, event type badge, date/time in Italian format, title, description preview"
    - "View toggle switches between grid and calendar views"
  artifacts:
    - path: "apps/web/src/components/events/EventCardGrid.tsx"
      provides: "Responsive CSS Grid layout for event cards"
      min_lines: 30
    - path: "apps/web/src/components/events/EventCard.tsx"
      provides: "Airbnb-style event card with image optimization"
      min_lines: 60
    - path: "apps/web/src/components/events/ViewToggle.tsx"
      provides: "Toggle button component for grid/calendar views"
      exports: ["ViewToggle"]
  key_links:
    - from: "apps/web/src/components/events/EventCard.tsx"
      to: "apps/web/src/lib/image-utils.ts"
      via: "Supabase image transformation helper"
      pattern: "getOptimizedImageUrl"
    - from: "apps/web/src/components/events/EventCardGrid.tsx"
      to: "apps/web/src/components/events/EventCard.tsx"
      via: "maps events to cards"
      pattern: "events.map.*EventCard"
---

<objective>
Create responsive, Airbnb-style event card grid components with view toggle functionality.

Purpose: Provide reusable UI components for displaying events in grid and calendar views. Cards optimize images via Supabase transformations and display Italian-formatted dates.

Output: EventCardGrid, EventCard, and ViewToggle components ready for use in unified discovery page.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ui-ux-refactor-for-events-management/06-CONTEXT.md
@.planning/phases/06-ui-ux-refactor-for-events-management/06-RESEARCH.md
@.planning/codebase/STACK.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STRUCTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Supabase image optimization utility</name>
  <files>
    apps/web/src/lib/image-utils.ts
  </files>
  <action>
    Create image transformation helper at apps/web/src/lib/image-utils.ts:

    Export function getOptimizedImageUrl(bucket: string, path: string, options: ImageTransformOptions): string
    - Parameters: bucket (e.g., 'event-images'), path (filename), options: { width?, height?, quality?, resize? }
    - Use Supabase Storage image transformation API endpoint
    - Read VITE_SUPABASE_URL from environment (already configured)
    - Construct URL: `${supabaseUrl}/storage/v1/render/image/public/${bucket}/${path}`
    - Append query params: width, height, quality, resize (cover/contain/fill)
    - Default quality: 80
    - Return optimized URL string

    Example usage:
    - Thumbnail: getOptimizedImageUrl('event-images', 'abc123.jpg', { width: 400, quality: 80 })
    - Full size: getOptimizedImageUrl('event-images', 'abc123.jpg', { width: 1200, quality: 90 })

    Reference RESEARCH.md "Supabase Image Transformation for Responsive Cards" section.
  </action>
  <verify>
    - Check apps/web/src/lib/image-utils.ts exists
    - Check function exports getOptimizedImageUrl with correct signature
    - Check function constructs Supabase render endpoint URL
    - Run `pnpm --filter @jamia/web build` to verify TypeScript compiles
  </verify>
  <done>
    - Image optimization utility created with Supabase transformations
    - Function accepts width, height, quality, resize parameters
    - Returns optimized image URL ready for responsive images
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Airbnb-style EventCard component</name>
  <files>
    apps/web/src/components/events/EventCard.tsx
  </files>
  <action>
    Enhance or create EventCard component at apps/web/src/components/events/EventCard.tsx:
    - Props: event (EventWithOrganizer type from @jamia/types)
    - Import Card, CardContent from @/components/ui/card
    - Import Badge from @/components/ui/badge
    - Import useTranslation from react-i18next
    - Import format from date-fns with Italian locale (it)
    - Import getOptimizedImageUrl from @/lib/image-utils

    Card structure (Airbnb-style):
    1. Hero image container (h-48 relative overflow-hidden)
       - Use img with srcSet for responsive images:
         - Thumbnail: 400w (for cards)
         - Full: 800w (for larger screens)
       - sizes attribute: "(min-width: 1024px) 33vw, (min-width: 768px) 50vw, 100vw"
       - Object-fit: cover, full width/height
       - Event type Badge positioned absolute top-2 right-2
         - Use eventTypeVariant mapping: jam=default, class=secondary, workshop=outline, convention=destructive

    2. CardContent (p-4):
       - Title (h3, font-semibold, text-lg, mb-2, line-clamp-2)
       - Date/time (text-sm, text-muted-foreground, mb-2)
         - Format: format(new Date(event.date), 'dd MMMM yyyy, HH:mm', { locale: it })
         - Example: "22 gennaio 2026, 14:30"
       - Description preview (text-sm, line-clamp-2)
         - Truncate to 150 characters

    3. Hover effects:
       - Add hover:shadow-lg transition-shadow classes
       - Smooth transition on card hover

    4. Click handler:
       - Navigate to `/events/${event.id}` on card click
       - Use useNavigate from react-router-dom

    DO NOT show distance on cards (per CONTEXT.md decision).
    Reference RESEARCH.md Pattern 5 for implementation details.
  </action>
  <verify>
    - Check EventCard.tsx exists in apps/web/src/components/events/
    - Check component uses getOptimizedImageUrl for hero image
    - Check srcSet provides 400w and 800w image variants
    - Check date formatting uses Italian locale (dd MMMM yyyy, HH:mm)
    - Check event type badge positioned in top-right corner
    - Run `pnpm --filter @jamia/web build` to verify component compiles
  </verify>
  <done>
    - EventCard displays hero image with Supabase optimization
    - Card shows event type badge, Italian date/time, title, description preview
    - Hover effects applied (shadow-lg transition)
    - Card navigates to event detail on click
  </done>
</task>

<task type="auto">
  <name>Task 3: Create EventCardGrid and ViewToggle components</name>
  <files>
    apps/web/src/components/events/EventCardGrid.tsx
    apps/web/src/components/events/ViewToggle.tsx
  </files>
  <action>
    Create EventCardGrid component at apps/web/src/components/events/EventCardGrid.tsx:
    - Props: events (EventWithOrganizer[])
    - Use CSS Grid with Tailwind classes: `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 auto-rows-fr`
    - Map events array to EventCard components
    - If events array empty: show empty state message (Italian: "Nessun evento trovato")
    - Import EventCard from ./EventCard

    Responsive breakpoints:
    - Mobile (0-767px): 1 column
    - Tablet (768px-1023px): 2 columns
    - Desktop (1024px+): 3 columns

    Create ViewToggle component at apps/web/src/components/events/ViewToggle.tsx:
    - Props: currentView ('grid' | 'calendar'), onViewChange ((view: 'grid' | 'calendar') => void)
    - Use Button component from shadcn/ui with ToggleGroup pattern
    - Two buttons: Grid view, Calendar view
    - Active view: variant="default", inactive: variant="outline"
    - Icons: Grid3x3 icon for grid view, Calendar icon for calendar view (from lucide-react)
    - Italian labels: "Griglia" (grid), "Calendario" (calendar)
    - Horizontal layout with gap-2

    Example usage:
    ```tsx
    <ViewToggle
      currentView={view}
      onViewChange={(newView) => setView(newView)}
    />
    ```

    Reference RESEARCH.md Pattern 4 for view toggle implementation.
  </action>
  <verify>
    - Check EventCardGrid.tsx exists with responsive grid classes
    - Check empty state message displays in Italian
    - Check ViewToggle.tsx exists with two button options
    - Check ViewToggle uses Italian labels (Griglia, Calendario)
    - Check active/inactive button variants differentiate visually
    - Run `pnpm --filter @jamia/web build` to verify components compile
  </verify>
  <done>
    - EventCardGrid renders events in responsive 1/2/3 column layout
    - ViewToggle provides grid/calendar view switching with Italian labels
    - Empty state displays Italian message when no events
    - Components ready for integration in unified discovery page
  </done>
</task>

</tasks>

<verification>
**Overall phase checks:**
- Run `pnpm --filter @jamia/web dev` and verify app starts
- Navigate to page using EventCardGrid component
- Verify cards display in 3-column grid on desktop, 2 on tablet, 1 on mobile (resize browser)
- Check event card images load optimized versions (inspect network tab for width params)
- Verify Italian date format displays correctly (e.g., "22 gennaio 2026, 14:30")
- Test ViewToggle: click buttons and verify active/inactive styling

**Must-haves validation:**
- Truth 1: Resize browser window - grid adapts from 1 to 2 to 3 columns
- Truth 2: Check card content - hero image, badge, Italian date, title, description visible
- Truth 3: Click ViewToggle buttons - active button highlighted, callback fires
</verification>

<success_criteria>
- Image optimization utility creates Supabase transformation URLs
- EventCard displays Airbnb-style layout with hero image, badge, Italian date/time
- EventCardGrid renders responsive grid (1/2/3 columns based on screen size)
- ViewToggle provides grid/calendar switching with Italian labels
- Cards navigate to event detail page on click
- Empty state displays Italian message when no events
</success_criteria>

<output>
After completion, create `.planning/phases/06-ui-ux-refactor-for-events-management/06-02-SUMMARY.md`
</output>
