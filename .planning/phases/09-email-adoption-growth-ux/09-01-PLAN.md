---
phase: 09-email-adoption-growth-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/src/email-preferences/email-preferences.service.ts
  - apps/web/src/components/dashboard/DigestNudgeBanner.tsx
  - apps/web/src/pages/Dashboard.tsx
  - apps/web/src/locales/it/common.json
autonomous: true

must_haves:
  truths:
    - "Existing user who never configured email preferences sees a dismissible banner on Dashboard"
    - "Clicking 'Attiva' in the banner enables monthly digest and hides the banner with toast confirmation"
    - "Dismissing the banner permanently hides it (even across page refreshes)"
    - "New users who complete ProfileSetup do NOT see the banner (digest already enabled)"
    - "Users who explicitly disabled digest in settings do NOT see the banner"
    - "Any preference update in backend sets has_seen_digest_prompt to true"
  artifacts:
    - path: "apps/web/src/components/dashboard/DigestNudgeBanner.tsx"
      provides: "Dismissible banner with one-click opt-in"
      min_lines: 40
    - path: "apps/backend/src/email-preferences/email-preferences.service.ts"
      provides: "Updated updatePreferences setting has_seen_digest_prompt: true"
      contains: "has_seen_digest_prompt"
  key_links:
    - from: "apps/web/src/pages/Dashboard.tsx"
      to: "apps/web/src/components/dashboard/DigestNudgeBanner.tsx"
      via: "conditional render based on preferences query"
      pattern: "shouldShowNudgeBanner.*DigestNudgeBanner"
    - from: "apps/web/src/components/dashboard/DigestNudgeBanner.tsx"
      to: "/api/email-preferences"
      via: "useMutation calling emailPreferencesApi.updatePreferences"
      pattern: "emailPreferencesApi\\.updatePreferences"
    - from: "apps/backend/src/email-preferences/email-preferences.service.ts"
      to: "email_preferences table"
      via: "Supabase update with has_seen_digest_prompt: true"
      pattern: "has_seen_digest_prompt.*true"
---

<objective>
Add a dashboard nudge banner for existing users who have never configured email preferences, and update the backend to mark preferences as "seen" on any update.

Purpose: Maximize email digest subscription rates for existing users through a non-intrusive, dismissible dashboard banner with one-click opt-in. Combined with the already-working ProfileSetup auto-save for new users, this completes the adoption flow for all user segments.

Output: DigestNudgeBanner component on Dashboard, backend `has_seen_digest_prompt` flag management, Italian translations.
</objective>

<execution_context>
@/Users/pietrodichio/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pietrodichio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-email-adoption-growth-ux/09-RESEARCH.md
@.planning/phases/09-email-adoption-growth-ux/09-CONTEXT.md
@packages/types/src/email-preferences.ts
@apps/web/src/api/email-preferences.api.ts
@apps/web/src/components/settings/EmailPreferencesCard.tsx
@apps/web/src/pages/Dashboard.tsx
@apps/backend/src/email-preferences/email-preferences.service.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend - set has_seen_digest_prompt on preference updates</name>
  <files>apps/backend/src/email-preferences/email-preferences.service.ts</files>
  <action>
    Modify the `updatePreferences` method in `EmailPreferencesService` to always include `has_seen_digest_prompt: true` in the update payload, regardless of what the DTO contains. This ensures that any user who interacts with email preferences (via ProfileSetup auto-save, Settings card, or the nudge banner) is marked as having "seen" the prompt and will not be nudged again.

    Specifically, change the `.update(dto)` call to `.update({ ...dto, has_seen_digest_prompt: true })`.

    Do NOT modify the `unsubscribeByToken` method -- unsubscribing via email link should not affect the prompt flag (user may re-subscribe later and should not be re-nudged).

    Do NOT modify the `getPreferences` method.
  </action>
  <verify>
    Run `pnpm run build --filter=backend` to confirm the backend compiles without errors. The change is a single line modification -- spread dto with has_seen_digest_prompt override.
  </verify>
  <done>
    The `updatePreferences` method always sets `has_seen_digest_prompt: true` alongside the provided DTO fields. Backend builds successfully.
  </done>
</task>

<task type="auto">
  <name>Task 2: Frontend - DigestNudgeBanner component, Dashboard integration, and translations</name>
  <files>
    apps/web/src/components/dashboard/DigestNudgeBanner.tsx
    apps/web/src/pages/Dashboard.tsx
    apps/web/src/locales/it/common.json
  </files>
  <action>
    **1. Create DigestNudgeBanner component** at `apps/web/src/components/dashboard/DigestNudgeBanner.tsx`:

    - Self-contained component that manages its own state (no props needed)
    - Uses `useQuery` with queryKey `['email-preferences']` to fetch preferences via `emailPreferencesApi.getPreferences` (reuses existing query, so data is shared with other components)
    - Visibility logic: show banner ONLY when ALL conditions are true:
      - Preferences query has loaded (`!isLoading`)
      - Preferences data exists (`preferences`)
      - `preferences.digest_enabled === false`
      - `preferences.has_seen_digest_prompt === false`
      - localStorage key `jamia_digest_prompt_dismissed` is NOT `'true'`
    - localStorage check must be synchronous in useState initializer to prevent flash:
      ```
      const [isDismissed, setIsDismissed] = useState(() => {
        try { return localStorage.getItem(STORAGE_KEY) === 'true'; }
        catch { return false; }
      });
      ```
    - Uses Shadcn `Alert`, `AlertTitle`, `AlertDescription` components
    - Uses `Mail` icon from lucide-react as the Alert icon
    - Uses `X` icon in a ghost Button for dismiss (absolute positioned top-right)
    - Uses `Button` with size="sm" for "Attiva" CTA
    - Mobile-first layout: `flex flex-col sm:flex-row sm:items-center gap-3` in AlertDescription
    - "Attiva" button: `flex-1 sm:flex-initial` for full width on mobile, auto on desktop
    - Dismiss handler: sets localStorage AND local state
    - Opt-in handler: `useMutation` calling `emailPreferencesApi.updatePreferences({ digest_enabled: true, digest_frequency: 'monthly' })`. On success: invalidate `['email-preferences']` query, set localStorage dismissed, set isDismissed true, show success toast. On error: show destructive toast.
    - All user-facing text uses `useTranslation('common')` with keys under `digestNudge.*`
    - Disable "Attiva" button while mutation is pending, show loading text
    - Alert styling: `relative mb-6 border-primary/20 bg-primary/5`
    - AlertTitle needs `pr-8` to avoid overlapping with dismiss button
    - Returns `null` if dismissed OR if shouldShow conditions are not met

    **2. Integrate DigestNudgeBanner into Dashboard.tsx**:

    - Import `DigestNudgeBanner` from `@/components/dashboard/DigestNudgeBanner`
    - Place `<DigestNudgeBanner />` BETWEEN the header section and the StatisticsSection, as a sibling in the `space-y-8` container
    - No conditional rendering needed in Dashboard -- the banner handles its own visibility internally
    - No new queries needed in Dashboard -- the banner manages its own data fetching

    **3. Add Italian translations** to `apps/web/src/locales/it/common.json`:

    Add a `digestNudge` section at the top level (same level as `emailPreferences`, `unsubscribe`, etc.):
    ```json
    "digestNudge": {
      "title": "Rimani aggiornato sugli eventi AcroYoga",
      "description": "Ricevi un riepilogo mensile degli eventi su Jamia direttamente via email.",
      "ctaButton": "Attiva",
      "loading": "Attivazione...",
      "successTitle": "Digest attivato!",
      "successMessage": "Riceverai un riepilogo mensile degli eventi su Jamia.",
      "errorMessage": "Impossibile attivare il digest. Riprova."
    }
    ```
  </action>
  <verify>
    1. Run `pnpm run build --filter=web` to confirm frontend compiles without errors
    2. Verify DigestNudgeBanner.tsx exists and exports the component
    3. Verify Dashboard.tsx imports and renders DigestNudgeBanner
    4. Verify common.json has the digestNudge translation keys
  </verify>
  <done>
    - DigestNudgeBanner renders on Dashboard for users with `digest_enabled: false` and `has_seen_digest_prompt: false`
    - Clicking "Attiva" enables monthly digest, hides banner, shows toast
    - Clicking X dismisses banner permanently via localStorage
    - Banner does NOT appear for new users (ProfileSetup auto-saves `digest_enabled: true`)
    - Banner does NOT appear for users who explicitly configured preferences (`has_seen_digest_prompt: true`)
    - All text is in Italian via i18n translations
    - Mobile layout stacks vertically, desktop is horizontal
  </done>
</task>

</tasks>

<verification>
1. Backend builds: `pnpm run build --filter=backend`
2. Frontend builds: `pnpm run build --filter=web`
3. Start dev servers: `pnpm run dev` -- Dashboard should load without errors
4. Manual check for existing user with `has_seen_digest_prompt: false` and `digest_enabled: false`: banner should appear
5. Manual check for user with `digest_enabled: true`: banner should NOT appear
</verification>

<success_criteria>
- DigestNudgeBanner component exists and is self-contained (manages own data fetching, visibility, mutations)
- Dashboard renders the banner between header and statistics
- Backend updatePreferences always sets has_seen_digest_prompt: true
- All user-facing text uses i18n translations
- localStorage prevents banner flash and persists dismissal
- Both backend and frontend build successfully
</success_criteria>

<output>
After completion, create `.planning/phases/09-email-adoption-growth-ux/09-01-SUMMARY.md`
</output>
